# ゴルフラウンド記録アプリ v1 仕様書

Claude Codeへの引き継ぎ用ドキュメント。同梱のモック3ファイルが**UI・ロジックの正解サンプル**なので、実装前に必ずブラウザで開いて挙動を確認すること。

| ファイル | 内容 |
|---|---|
| `index.html` | ラウンド中の入力画面(1ホール分のフロー一式) |
| `review.html` | ラウンド直後の振り返り画面(ダミー1ラウンド分を実ロジックで集計) |
| `dashboard.html` | 累積ダッシュボード(8ラウンド分のダミーデータ) |

---

## 1. コンセプト

- 目的は「記録」ではなく**診断→処方**。ラウンド記録から練習すべき課題を数字で特定する。
- ユーザーはまず開発者本人(iPhone、楽天GORAからの乗り換え)。将来の一般公開を見据えるが、v1はシングルユーザー。
- 入力は**プレー中にスマホで、1打あたり最大2〜3タップ**。これを超える入力は仕様として却下する。

## 2. 技術方針

- **PWA**(ホーム画面追加で使用)。ターゲットはiPhone Safari。
- 構成はシンプルに:フレームワークなし or 軽量構成。ビルドが壊れたときに非エンジニアが復旧できないので、依存は最小限にする。
- データは**端末内のIndexedDB**に保存。サーバー・アカウント機能なし。
- **オフラインで完全動作**(Service Worker)。ゴルフ場は電波が悪い。
- **バックアップのエクスポート/インポートは必須機能**(JSONファイルのダウンロード/読み込み)。SafariのストレージはOSに消されることがあるため。設定画面に配置し、ラウンド保存時に「最終バックアップからN日」の注意表示を出す。
- ホスティング:Vercel or GitHub Pages(無料枠)。
- 画面の見た目・配色はモックに準拠(緑基調 `#0f7b4d`)。

## 3. データモデル

```
Settings {
  clubs: string[]        // 使用クラブ。マスター(1W,3W,5W,7W,UT,2I..9I,PW,AW,SW,LW,PT等)から選択
                         // 入力画面のクラブグリッドはこの設定順に表示。PTは常に含む
  kpis: string[]         // 表示KPIのID配列。v1は既定9個固定だが、この形式で保持し
                         // v2でカスタマイズUIを開放する
}

Course {
  id, name: string
  pars: number[18]       // ラウンド中に確定・更新される(後述)
}

Round {
  id, date: string
  courseId: string
  tee: "赤" | "白" | "青" | "黒" | その他文字列   // ラウンド開始時に選択
  holes: Hole[18]
}

Hole {
  number: 1..18
  par: 3 | 4 | 5
  shots: Shot[]
}

Shot {
  club:   string                    // "1W".."SW" | "PT"
  lie:    "ティー"|"FW"|"ラフ"|"バンカー"|"林"|"グリーン"
  result: string                    // 9グリッドのラベル or 特殊("OB左","OB右","チョロ/ダフリ","チップイン","カップイン")
  kind:   "normal"|"nice"|"miss"|"ob"|"in"|"putt"
  dist?:  "ロング"|"ミドル"|"ショート"|"1m以内"   // club==="PT" のとき毎打必須
}
```

**スコアはShot配列から導出する(別途入力しない)**:
- ホールスコア = shots数 + ペナルティ数(kind==="ob"のshot数。OB=1打罰で打ち直しは別shotとして記録される)
- パット数 = club==="PT" のshot数

## 4. 画面仕様

### 4.1 ラウンド開始画面

- コース選択:過去のコース一覧(名前+ラウンド回数)から選択 or 新規コース名を入力。
- ティー選択:赤/白/青/黒+自由入力。
- 新規コースのパー構成は**事前入力させない**。全ホールPar4で開始し、プレー中にホール画面で確定する(次項)。一度確定したコースは次回から自動で設定される。ラウンド中にparを変更した場合はコースマスターにも反映。

### 4.2 入力画面(`index.html` 準拠)

構成(上から):ヘッダー / 打履歴バー / ライ行 / 9グリッド+特殊ボタン / クラブグリッド。

- **ヘッダー**:ホール番号・Par・次のショット番号。**Par表示はタップで3→4→5をトグル**(コースのpar確定手段。モック未実装なので新規実装)。
- **打履歴バー**:記録済みの打をチップで横並び表示。「1打戻す」ボタンで直前の打を取り消し。
- **ライ行**:ティー/FW/ラフ/バンカー/林 の5チップ。自動推測で選択済み(ルールは§5)。手動タップで上書き可。パット時は非表示。
- **9グリッド**:縦軸=オーバー/ジャスト/ショート、横軸=左/中/右。中央は「ナイス(狙い通り)」でkind="nice"。タップで1打記録。
- **特殊ボタン**:OB左・OB右(kind="ob"、+1罰)/チョロ・ダフリ(kind="miss")/チップイン(kind="in"、記録後ホールアウト)。
- **クラブグリッド**:設定のクラブ一覧を2段グリッドで常時表示(横スクロール禁止)。選択中をハイライト。ホール開始時は1Wを自動選択。
- **パットモード**(PT選択時):ライ行の代わりに**距離チップ4択**(ロング10m〜/ミドル3〜10m/ショート1〜3m/1m以内)。1打目はミドル、2打目以降は「1m以内」を自動選択、タップで変更可。9グリッド(中央=「惜しい」)+カップインボタン。カップインでホールアウト。
- **ホールアウトシート**:スコア/パット/ペナルティ+全打リスト。各打に**「修正」ボタン**:シートを閉じて入力画面が修正モード(黄色バナー+元の値がプリセット)になり、選び直すとその1打だけ差し替え。最終打をカップイン以外に修正した場合はホールアウト前状態に戻る。「次のホールへ」で次ホールに進む。
- 記録時のフィードバック:中央にトースト表示(クラブ+結果)、可能なら振動。
- 18ホール目のホールアウトでラウンド保存→振り返り画面へ遷移。途中終了(9ホール等)も保存可能にする。

### 4.3 振り返り画面(`review.html` 準拠)

上から:
1. ヘッダー:日付・コース・ティー、スコア(大)、±Par、パット/OB/3パットチップ。
2. **スコアロス要因TOP3と処方**:§6.4の計算。処方文は断定を避けた控えめなトーンにする。
3. **KPIタイル9個**(3×3):§6.2の指標。
4. **ホールタイプ別 平均±**(Par3/4/5)+「1ラウンドではブレが大きい」注記。
5. **距離帯別パット成績**(新規追加、モック未実装):距離帯ごとの「本数/カップイン率/3パット関与」を小テーブルで。
6. **ミス傾向ヒートマップ**:タブ=ドライバー/ウッド・UT/アイアン/ウェッジ/クラブ別。9マスに%と球数、緑の単色濃淡(濃=多)。読み解き文を自動生成(§6.5)。10球未満は「参考程度」注記。パットの外し方は別枠で常設。
7. ホール別テーブル:H/Par/スコア/±(色分け)/パット(3パットは赤)/メモ(OB・3パット・ダフリ・バンカー・寄せワン等を自動表示)。

### 4.4 累積ダッシュボード(`dashboard.html` 準拠)

1. ヘッダー:直近3R平均・ベスト・全期間平均。
2. **スコアとパット数の推移**:1カード内に上下2段の折れ線(上=スコア、下=パット)、日付軸は下段のみ。**2軸グラフは禁止**。表示は直近20Rまで。ベストスコアに★。
3. **主要指標9個**:直近3R平均と、その前3Rとの差分(▲▼、良化=緑/悪化=赤。パット・3パット・ペナルティは減が良化)。
4. ホールタイプ別 平均±(全期間)。
5. **コース別・ホール別 平均±**:コースをチップで切替、18ホールのタイル(番号・Par・平均±)。ワーストは赤背景、ベストは緑数字。読み解き文(要対策ホールと期待短縮打数)。
6. **距離帯別パット成績**(累積版。新規追加)。
7. 累積ミス傾向ヒートマップ:振り返りと同じ部品。累積なのでクラブ別が本領。

### 4.5 設定画面(新規実装)

- 使用クラブの選択(マスターからON/OFF。並び順はマスター順)。変更しても過去データは不変。
- バックアップ:エクスポート(JSON)/インポート。最終バックアップ日時表示。
- (内部のみ)KPI定義リスト。編集UIはv2。

### 4.6 画面遷移

ホーム(ラウンド一覧+「ラウンド開始」+ダッシュボード/設定への導線)→ ラウンド開始 → 入力(×18ホール)→ 振り返り。過去ラウンドの振り返りはホームの一覧から再表示可。

## 5. 自動推測ルール

**ライ**(次の打のデフォルト。手動で上書き可能):
- 1打目 → ティー
- 直前がOB → 同じライ(打ち直し)
- 直前がチョロ/ダフリ(kind="miss") → 同じライ
- 直前の結果に「左」or「右」を含む → ラフ
- それ以外(中央列:ナイス/オーバー/ショート) → FW
- バンカー・林は自動推測しない(手動選択のみ)

**パット距離**:1打目=ミドル、2打目以降=1m以内 をデフォルト選択。

## 6. 集計定義(すべてShot配列から導出)

### 6.1 ホール単位
- スコア、パット、ペナルティ:§3の通り
- グリーン到達打数 toGreen = (shots数 − パット数 + ペナルティ数)
- パーオン(GIR):toGreen ≤ par−2
- ボギーオン:toGreen ≤ par−1
- 寄せワン:GIRでない かつ スコア ≤ par
- FWキープ:par≥4のホールで、ティーショットがOBでなく、結果に「左」「右」を含まない
- サンドセーブ:バンカーのライで打った かつ スコア ≤ par
- 3パット:パット ≥ 3

### 6.2 KPI 9個(振り返り=当日値、ダッシュボード=直近3R平均+前3R比)
パット合計、FWキープ%、パーオン%、ボギーオン%、寄せワン(成功/機会)、サンドセーブ(成功/機会、機会0なら「-」)、3パット回数、ペナルティ打数、ナイス率(パット以外の全ショット中kind="nice"の割合。分母にob/missを含み、"in"は除く)。

### 6.3 ヒートマップ集計
- 対象:kind が normal/nice/putt(ob・miss・in は除外)
- マス割り:結果文字列に「オーバー」を含む→上段、「ショート」→下段、他→中段。「左」を含む→左列、「右」→右列、他→中列(「左に外し」は中段左列になる。この文字列規則を崩さないこと)
- クラブグループ:ドライバー=[1W] / ウッド・UT=[3W,5W,UT] / アイアン=[5I..9I] / ウェッジ=[PW,AW,SW] / クラブ別=1本ずつ(設定のクラブ構成に追従)
- 色:緑単色の濃淡7段階、最大セル基準で正規化。濃いセルは白文字

### 6.4 スコアロスTOP3(振り返り)
- OB:1発 = −2打換算(打ち直し+罰の概算)
- チョロ/ダフリ:1回 = −1打
- 3パット:1回 = −1打(2パット比)
- 大きい順に3件。件数0の項目は出さない。各項目に方向・ライ等の内訳(「すべて右」等)と処方文を添える。処方文は一般論になりすぎないよう控えめに(「〜の可能性。」「〜をチェック。」程度)

### 6.5 ヒートマップ読み解き文
- nice率(中央セル/全体)を必ず表示
- ミス(中央以外)の最大セルがミスの34%以上 → 「ミスは◯・◯に集中(ミスのN%)」
- そうでなく、行・列合計(オーバー系/ショート系/左系/右系)の最大がミスの50%以上 → 「ミスは◯◯系に偏り」
- どちらでもない → 「大きな偏りはありません」

### 6.6 距離帯別パット
距離帯(4区分)ごとに:試行数、カップイン率(その距離から1打で沈めた率)、平均残りパット数。ロング/ミドル起点の3パット率も算出。

## 7. v1に入れないもの(v2以降)

- KPIカスタマイズの編集UI(内部データ構造だけv1で用意)
- アプローチの残り距離・ピンまでの距離記録
- 練習記録との連携、傾斜・風・ピン位置の記録(意図的に非対応。入力負担を増やさない)
- コースDBのAPI連携、ネイティブアプリ化、複数ユーザー・共有機能

## 8. 受け入れ確認の観点

1. モック3画面と同じ操作フロー・同じ集計結果になること(review.htmlのダミーデータを入れたら同じ数字が出る、が理想の検証)
2. 機内モードで18ホール入力→保存→振り返り表示ができること
3. 1打の記録が最大3タップ以内(クラブ+ライ修正+グリッド)であること
4. バックアップJSONをエクスポート→全消去→インポートで完全復元できること
5. ホールアウト後の「修正」で任意の1打を差し替えてもスコア・集計が正しく再計算されること
