# 作業ログ(fix-instructions-3.md / fix-instructions-4.md 自走セッション)

このファイルは `fix-instructions-3.md`(バッチ6〜10)・`fix-instructions-4.md`
(バッチ11〜14)の指示に従い、ユーザー不在のセッションで実施した記録。
各バッチの終了時に追記する。

---

## バッチ6:集計バグの修正3件(完了)

対象: `js/stats.js` のみ。

### 実施内容

- **6-1**: `dashboardSummary(allRounds, courses, opts)` に `opts.roundFilter`
  (既定 `"18"`)を追加し、既定では `playedHoleCount(r) === 18` のラウンドのみを
  集計対象にした。`opts.roundFilter === "all"` を渡すと9ホールラウンドも含める
  (バッチ8の絞り込みUIから利用する前提のインターフェース)。
- **6-2**: `holeStats` の `fwKeep` 判定に `tee.kind !== "miss"` を追加。チョロ・
  ダフリのティーショットがフェアウェイキープに数えられなくなった。
- **6-3**: グリーンサイドバンカー判定 `hasGreensideBunker(shots)` を新設(バンカー
  ライのショットの次がパット、またはそのショット自体がカップイン、のとき
  グリーンサイドとみなす)。新フィールド `gsBunker` を `holeStats` の戻り値に追加し、
  `sandSave` の判定・`computeKPIs`/`dashboardSummary` の母数をすべて `gsBunker` に
  切り替えた。**表示用の `bunker` フィールド(ホール別テーブルの「バンカー」注記に
  使用)は従来どおり「そのホールでバンカーのライから打った回数」のまま変更していない**
  (指示範囲がサンドセーブの母数修正に限定されていたため、注記表示への影響を避けた)。

### 動作確認

1. ブラウザコンソールで `js/stats.js` を動的importし、以下をユニットテスト的に検証:
   - チョロ・ダフリのティーショット → `fwKeep === false`
   - フェアウェイバンカー(バンカーショットの次が通常ショット)→ `gsBunker === false`,
     `bunker === true`(表示注記は維持), `sandSave === false`
   - グリーンサイドバンカー(次がパット)→ `gsBunker === true`, パー以下なら `sandSave === true`
   - OBの罰打計算: `adv:true` → pen=2 / `adv:false` → pen=1 / `adv`未設定(既存データ)→ pen=1
   - `dashboardSummary`: 9ホール完了ラウンド+18ホール完了ラウンドを渡すと、既定
     (`opts`なし)では18ホールのみ(`perRound.length===1`)、`roundFilter:"all"`では
     両方(`perRound.length===2`)
   - すべて期待通りの結果。
2. 実際に `js/db.js` 経由で18ホール完了ラウンド・9ホール完了ラウンドをIndexedDBに
   シードし、`dashboard.html` と `review.html` をモバイル幅で実表示して確認。
   - ダッシュボードの「ホールタイプ別 平均の±全1ラウンド」の表示から、9ホール
     ラウンドが除外され18ホールラウンドのみ集計されていることを確認。
   - コンソールエラーなし。既存の表示(スコア推移・KPI・ヒートマップ・コース別)は
     いずれも壊れていない。

### 判断に迷った点

- `bunker` フィールドの意味を変更するかどうかで迷った。サンドセーブの母数だけを
  正しくする目的なら `bunker` 自体を「グリーンサイドのみ」に変えてしまう手もあったが、
  それだとホール別テーブルの「バンカー」注記(フェアウェイバンカーも含めた事実表示)の
  意味が変わってしまい、指示書が触れていない画面表示に影響が出る。指示範囲を
  超えないため、新フィールド `gsBunker` を追加する方式にした。

### スキップした項目

なし。6-1/6-2/6-3すべて実施。

---

## バッチ7:大ミスの方向記録と13マスヒートマップ(完了)

着手前に `mock/input13.html`(方向入力フロー)と `mock/heatmap13.html`(13マス
ヒートマップ)をブラウザで開いて挙動を確認済み。実装はこの2モックのロジック・
文言に合わせている。

### 実施内容

**7-1・7-1b(`hole.html` / `js/holeInput.js`)**

- 特殊ボタンを「OB / ペナ(赤杭・池) / トップ・チョロ / チップイン」の4つに統合
  (旧「OB左」「OB右」「ペナルティ」「チョロ/ダフリ」を置き換え)。
- OB・ペナを押すと9グリッドが方向選択パネルに変わる(奥/左/右/手前の4マスのみ有効・
  赤系強調、他5マスは無効化)。特殊ボタン行は「やめる」ボタンに差し替え。
- OBかつライ=ティーのときだけ、方向確定後に「打ち直し(+1罰)/前進(+2罰)」の
  2択を挟む。2打目以降のOB・ペナはその場で即記録。
- Shotに `dir`(左/右/奥/手前)・`adv`(前進フラグ、OBのみ意味を持つ)を追加。
- `js/stats.js`: `holeStats` の罰打計算を `ob: adv?2:1` `penalty: 常に1` に修正
  (旧実装は両方一律1打罰だった)。`inferLie` はOB前進なら次ライ=FW、打ち直しなら
  元のライに戻すよう修正。
- 履歴チップ・ホールアウトシートに「(前進)/(打直)」併記、記録直後のトーストに
  「前進 +2打罰」「打ち直し +1打罰」を表示。
- 入力画面の「ライ」「距離」ラベルを削除(チップの文言のみで判別可能なため)。

**7-2(`js/stats.js` / `js/review.js` / `js/dashboard.js` / `css/style.css`)**

- 13マスヒートマップ(内側9マス+外周4マスの大ミス方向)を実装。母数はパット以外の
  全ショットからkind="in"を除いたもの(13マス合計=母数)。
  `heat13Shots`/`build13Heat`/`heat13Total`/`heatmap13InsightHTML` を新設し、
  振り返り・累積ダッシュボード両方のショットヒートマップをこれに置き換え
  (パットのヒートマップは従来の9マスのまま変更なし)。
- 内側9マスは既存の緑ランプ(RAMP)、外周4マスは新設の赤茶ランプ(RAMP_RED)で
  それぞれのグループ内最大値により正規化。
- 軸ラベル(オーバー/ジャスト/ショート、左/中/右)はショットヒートマップのみ削除
  (パットのヒートマップは維持)。
- 読み解き文はモックの文面(ナイス率→最多ミスマス→大ミス内訳)に合わせた。

**既存データのマイグレーション(非破壊)**

- 旧データ(dir未記録の"OB左"/"OB右")は、書き込みを行わず読み取り時に
  `shotDir(s)` で結果文字列から方向を導出する方式にした(DBへの書き戻しをしない
  ぶん、より安全・より単純)。dir未記録の旧「ペナルティ」は導出もできないため
  そのまま`null`とし、13マスヒートマップでは仕様どおり「手前へ大ミス」に合流させた。
  `adv` 未設定の旧OBショットは falsy 判定で自然に「打ち直し(+1罰)」として扱われる
  (コード上、明示的なデフォルト値書き込みは不要だった)。

### 動作確認

1. `mock/input13.html` と `mock/heatmap13.html` を実ブラウザ(モバイル幅)で開き、
   レイアウト・配色・文言を確認してから実装した。
2. ブラウザコンソールで `stats.js` の新関数を単体テスト(方向別カウント・合計が
   ショット数と一致すること、読み解き文の内訳表示など)。
3. `hole.html` を実際に操作して確認(JS経由でクリックイベントを発火。ブラウザ操作
   ツールの `computer` アクションが本セッション内で断続的にタイムアウトしたため、
   `javascript_tool` でのDOMイベント発火+スクリーンショットで代替した):
   - OB→方向(右)→前進を選択 → 次打が4打目になり、ライが自動でFWになることを確認
     (受け入れ確認4を満たす)。
   - 2打目以降のOB(方向:左)→2択が出ず即座に「打ち直し(+1罰)」で記録されることを確認。
   - 「やめる」でOBピック中に記録なしでキャンセルできることを確認。
   - ペナ→方向(奥)→ライが「ラフ」に自動設定されることを確認。
   - ホールアウトシートの罰打タグ(+2罰/+1罰)、履歴チップの(前進)/(打直)表示を確認。
   - パット距離選択画面(PT選択時)で「距離」ラベルが表示されないことを確認。
4. 実データで振り返り・累積ダッシュボードの13マスヒートマップを確認:
   - 13マス合計がショット数(母数)と一致すること(例:全14球のとき9マス+4バーの
     合計が14になることをコンソールで確認)。
   - クラブ別・クラブグループ別タブを切り替えても正しく再集計されること。
5. **既存データ互換性**: dir/adv フィールドが存在しない旧形式のOB・ペナルティ
   ショット(`{kind:"ob", result:"OB左"}` など)を含むラウンドを保存し、振り返り
   画面を表示。罰打計算(1打罰として扱われる)・ヒートマップの方向振り分け
   (結果文字列から左/右を導出)ともに正しく、コンソールエラーもなし。

### 判断に迷った点

- 旧OB左/OB右の `dir` 補完を「DBへの書き込みを伴うマイグレーション」にするか
  「読み取り時の導出のみ」にするかで迷った。前者は `ensurePresetCourses` と
  同じパターンだが、ラウンドデータの書き換えは慎重を期すべき(共通条件の
  「非破壊・冪等」原則)と考え、書き込みを伴わない導出方式を採用した。
  実データを変更しないため、何度表示しても安全(冪等)。
- 13マスヒートマップの読み解き文で「大ミスが0件のときも『ミスで最も多いのは
  ○○(ミスの0%)』と出る」挙動は一見不自然だが、`mock/heatmap13.html` 自体が
  同じロジックであり(ミス0件を特別扱いしていない)、モックを正解サンプルとする
  指示に従いそのまま踏襲した。

### スキップした項目

なし。7-1/7-1b/7-2/7-3すべて実施。

---

## バッチ8:ダッシュボードの絞り込み(完了)

### 実施内容

- `dashboard.html` のヘッダー直下(`<main>`先頭)に絞り込み行を追加。コース/ティー/
  ラウンド(18Hのみ既定・すべて)の3つを`<select>`で選択する形にした。
- コース・ティーの選択肢は「記録に存在するものだけ」を動的に構築(完了ラウンドの
  `courseId`/`tee`から重複排除)。バッチ6-1で追加した`roundFilter`と合わせて、
  `js/stats.js` の `dashboardSummary(allRounds, courses, opts)` に
  `opts.courseId`・`opts.tee` を追加し、3条件で複合フィルタできるようにした。
- `js/dashboard.js` を、初回描画だけだった構造から `renderAll()` 関数に包んで
  絞り込み変更のたびに再実行できる形にリファクタ(各コンテナのinnerHTMLクリア、
  タブ状態(コース選択インデックス・クラブグループ選択)を再描画ごとにリセット)。
  スコア推移・主要指標・ホールタイプ別・コース別・距離帯別パット・ヒートマップの
  すべてが連動して絞り込まれる。
- 各カードの見出しに `該当Nラウンド` を表示(`<small class="filter-count">`を
  各h2に追加し、`renderAll()`内で一括更新)。
- 該当0ラウンドのときは「条件に合うラウンドがありません。」を表示し、
  グラフやKPIタイルは描画しない(絞り込み行自体は表示したまま、条件を
  変えられるようにした)。完了ラウンドが1件も無い場合の元々のメッセージ
  (「完了したラウンドがまだありません」)は区別して維持し、その場合は
  絞り込み行ごと非表示にする(選ぶものが無い状態でUIを出しても無意味なため)。
- 選択状態は `localStorage`(キー `golf-log:dashboard-filter`)に保存し、
  次回開いたときも復元する。

### 動作確認

1. コース違い・ティー違い・9ホール/18ホール混在の複数ラウンドを実データとして
   IndexedDBにシードし、`dashboard.html` をモバイル幅で操作。
2. コース選択肢が「登録済みコースのうち完了ラウンドがあるもの」だけになっている
   ことを確認(未使用コースは出ない)。ティーも記録に存在する値(赤・白)のみ
   出て、記録にない色(青・黒)は出ないことを確認。
3. コース→旧コース、ティー→赤 のように条件を重ねて絞り込み、ヘッダーの日付範囲・
   ラウンド数・各カードの「該当Nラウンド」がすべて連動して変わることを確認。
4. ラウンド:すべて に切り替えると9ホールラウンドも合算されることを確認
   (バッチ6-1の既定動作と合わせて正しく機能)。
5. 該当0件になる組み合わせ(旧コース×赤ティー、実際には存在しない組み合わせ)を
   選択し、「条件に合うラウンドがありません。」が表示されグラフ等が描画されない
   ことを確認。絞り込み行自体は表示されたままで条件を戻せることを確認。
6. ページを再読み込みし、選択した絞り込み条件(コース/ティー/ラウンド)が
   `localStorage` から復元されていることを確認。
7. 完了ラウンドを全削除した状態でページを開き、絞り込み行が非表示になり、
   従来どおり「完了したラウンドがまだありません」の空表示になることを確認。
8. コンソールエラーなし。

### 判断に迷った点

- 指示書では「ティー:…(記録に存在するものだけ出す)」とだけ書かれており、
  「コース」側にも同じ注記は無かった。ただし絞り込みUIとしての一貫性を考え、
  コースの選択肢も「完了ラウンドで実際に使われているコースだけ」に限定した
  (未使用の登録コースを選んでも常に「該当0ラウンド」にしかならず、選択肢として
  出す意味が薄いため)。

### スキップした項目

なし。

---

## バッチ9:KPIタイルの選択式と比較対象(完了)

### 実施前に気づいた懸念点(重要・ユーザー確認推奨)

着手前に `CLAUDE.md` を再確認したところ、「v1に入れない範囲(SPEC §7)」の項に
**「KPIカスタマイズUI…は意図的に対象外」**と明記されているのを見つけた。これは
今回のバッチ9(設定画面でKPIタイルを選べるようにする)と正面から矛盾する。

判断: `CLAUDE.md`の「次にやること」節が`fix-instructions-3.md`を名指しで「次に
やるべき作業」として案内しており、そのバッチ9に本機能が明記されていること、
また作業再開時にユーザー(を代理するオーケストレーター)からもバッチ9・10を
続行するよう明示的な指示があったことから、「v1範囲外」の記述は
`fix-instructions-3.md`作成時点でユーザー自身が方針転換した後に更新し忘れた
古い記述である可能性が高いと判断し、作業を続行した。**ただしこれは私の推測であり、
ユーザーが確認した方が良い。** 本機能を正式に採用するなら、`CLAUDE.md`/`SPEC.md`の
「v1に入れない範囲」からKPIカスタマイズUIの記述を削除することを推奨する。

### 実施内容

**9-1(`js/stats.js` / `js/db.js` / `js/settings.js` / `settings.html`)**

- 指示書に列挙された26候補すべてを `KPI_CATALOG`(id・グループ・ラベル・単位・
  種別・良し悪しの向き・calc関数)としてカタログ化。既定の★9個は
  `clubs.js`の`DEFAULT_KPIS`と一致させた(後方互換)。
- 値の種別を4種に整理: `rate`(%、分子/分母付き)・`count`(整数)・
  `avg`(平均、母数付き)・`diff`(符号付き差分)。差分系(前半/後半スコア差、
  FWキープ時とミス時のスコア差)は「良し悪しの向きが一意に決まらない」ため
  `lowerBetter: null` とし、比較の▲▼色付けを行わない(中立表示)扱いにした。
- `computeKPIs(holes, kpiIds)` を設定のkpiIds配列に対応させ、review用に
  分子/分母付きラベルを生成。`kpiRoundValue(id, holes)` を新設し、ダッシュボードの
  ラウンド単位平均化にも同じカタログ・同じ計算式を使い回せるようにした
  (計算式の二重管理を避けた)。
- `dashboardSummary` の主要指標ブロックを、旧来のハードコードされた9指標専用の
  フィールド(fwPct/girPct/…)から、`opts.kpiIds` に基づく汎用計算に置き換えた。
- 設定画面に「表示する指標」カードを追加。9つの枠それぞれに、グループ分けした
  `<select>`(optgroup)で26候補から選べるようにした。同じ指標を既に使っている
  枠があれば、その枠と選択内容を入れ替える(重複を許さない)。並べ替えは
  「どの枠にどの指標を割り当てるか」を変えることで実現する方式にした
  (ドラッグ&ドロップ等の専用UIは作らず、既存の「選ぶ→反映」の操作感に寄せた)。
- `js/db.js` の `getSettings()` に非破壊マイグレーションを追加。既存の設定
  レコードに `kpis` フィールドが無い(または不正な)場合のみ既定値を補う
  (`clubs`等の他フィールドは変更しない。冪等)。

**9-2(`review.html` / `js/review.js`)**

- 振り返り画面のKPIカード上部に比較対象セレクタ(直近5R平均・全期間平均・
  自己ベストのラウンド・同じコースでの平均・前回のラウンド)を追加。
- `compareRoundsFor(mode, allRounds, currentRound)` / `compareKpiValues(rounds, kpiIds)`
  を新設。現在表示中のラウンド自身は比較対象から常に除外する。
- 各KPIタイルに比較値を併記し、良い方向の差を緑・悪い方向を赤で色付け
  (中立指標は色を付けない)。比較対象が存在しない・母数不足(null)のときは
  併記自体を省略する。
- 選択はlocalStorageに保存し次回も復元する。

### 動作確認

1. `KPI_CATALOG`(26件)をブラウザコンソールから直接呼び出し、意図的に組んだ
   18ホール分のテストデータ(OB・フェアウェイバンカー・ラフ・トラブルショット・
   3パットなしなど諸条件を混在させたもの)に対して26指標すべての計算結果を
   手計算した値と突き合わせ、**全26件が一致**することを確認(パーセーブ率・
   バーディ数・前半/後半差・FWキープ率・ティーショット無事率・FWキープ時と
   ミス時のスコア差・ナイス率・クラブ別ナイス率・ラフからのグリーンオン率・
   パー3のパーオン率・寄せワン・サンドセーブ・トラブル脱出率・パーオンホールの
   平均パット数・1m以内の成功率など)。
2. 設定画面でKPI枠の1つを既に他の枠にある指標に変更 → 元の枠の指標と入れ替わる
   (重複しない)ことを確認。IndexedDBへの永続化も確認。
3. 設定でカスタム構成(ダボ以上・パーオン率など★以外の指標を含む)にしたのち、
   振り返り画面・累積ダッシュボードの両方に同じ構成が反映されることを確認。
4. 比較対象を「直近5ラウンドの平均」→「同じコースでの平均」→「前回のラウンド」と
   切り替え、各タイルの併記値・色(緑/赤/中立グレー)が連動して変わることを確認。
   母数不足(サンドセーブの機会なしなど)のタイルは併記が出ないことを確認。
   選択後にページを再読み込みし、localStorageから復元されることを確認。
5. `diff`種別(前半/後半スコア差、FWキープ時とミス時のスコア差)・`avg`種別
   (パーオンホールの平均パット数)を実際に9枠に設定し、値が"+0打"
   "(×14H)"のように正しくフォーマットされ、コンソールエラーなく表示されることを
   確認。null(計算不能)のときは"-"で安全に表示されることも確認。
6. 既存設定(`kpis`フィールードが無い形式)をIndexedDBに直接投入し、
   `getSettings()`が非破壊的に補完すること(他フィールドは変更されない)、
   2回連続で呼んでも結果が変わらない(冪等)ことを確認。
7. コンソールエラーなし。

### 判断に迷った点

- 「並べ替えもできること」の実現方法として、ドラッグ&ドロップ等の専用UIは
  作らず、9つの枠それぞれで指標を選び直す方式(重複時は入れ替え)にした。
  結果として「どの指標をどの枠(=表示順)に置くか」は自由に決められるため、
  要件は満たしていると判断したが、直感的な「並べ替え」操作感とは異なる可能性がある。
- 比較対象の各モードの厳密な定義(直近5R=このラウンドより前の直近5R、全期間・
  同コース平均=このラウンド自身を除く、前回=日付が厳密に前の直近1件)は
  指示書に明記が無かったため妥当と考えられる定義を自分で決めた。同日に複数
  ラウンドがある場合は日付文字列の比較のみで判定するため、直近5R/前回の対象に
  含まれないことがある(簡略化)。
- 差分系KPI(前半/後半スコア差、FWキープ時とミス時のスコア差)は「良し悪しの
  向き」が一意に決まらないと判断し、ダッシュボード・振り返り両方で色付けをせず
  中立表示にした(指示書に明記は無いが、事実ベースの数字表示という既存方針に
  沿った判断)。

### スキップした項目

なし。9-1・9-2とも実施。

---

## バッチ10:スコア分布帯とバックアップ導線(完了)

### 実施内容

**10-1(`js/stats.js` / `review.html` / `dashboard.html` / `js/review.js` / `js/dashboard.js` / `css/style.css`)**

- `scoreDistribution(holes)` を新設。区分はイーグル以下/バーディ/パー/ボギー/
  ダボ/トリプル以上の6区分。振り返り(そのラウンド)・累積ダッシュボード
  (絞り込み後の全ホール)の両方に同じ関数を使い、`computeReview`/`dashboardSummary`
  の戻り値に `scoreDist` として追加した。
- 表示は横1本の積み上げ帯(`.dist-bar`、各区分の幅=割合%)+ 直下に凡例
  (色スウォッチ・区分名・ホール数・割合%)。色は1系統の緑の濃淡
  (`DIST_RAMP`、良い側ほど薄く、悪い側ほど濃い)。助言文は付けていない。
  ダッシュボード側はバッチ8の絞り込みと連動する(該当ラウンド数表示も付与)。

**10-2(`js/holeInput.js` / `review.html` / `js/review.js` / `js/home.js` / `css/style.css`)**

- `hole.html` の2つの保存完了経路(18ホール完了・ここまでで終了して保存)の
  `review.html` へのリダイレクトURLに `&saved=1` を付与。
- 振り返り画面は `saved=1` のときだけ最上部に「バックアップを保存」ボタン
  (`js/backup.js`の`exportBackup()`を流用、設定画面と同じ処理)を表示する。
  過去のラウンドを普段閲覧するときには出ない。
- ホーム画面の警告バナーを2段階にした: (1) 最終バックアップ以降に**保存された
  完了ラウンドが3件以上**たまっている場合は赤背景・太字の強調表示
  (「今すぐバックアップしてください。」)、(2) それ未満だが**最終バックアップから
  7日以上**経過している場合は従来どおりの控えめな表示。両方の条件に該当する
  場合は(1)を優先する。

### 動作確認

1. コンソールで `scoreDistribution` に6区分すべてが混在するテストデータを渡し、
   各区分への分類(par-2以下/-1/0/+1/+2/+3以上)が正しいことを確認。
2. 実際に18ホール分のラウンドを保存し、振り返り画面でスコア分布の帯・凡例が
   正しい件数・割合(合計18ホール)で表示されることを確認。
3. `hole.html` から実際に最終ホールをプレーして保存完了 → `review.html` に
   `saved=1` 付きで遷移し、バックアップCTAが表示されることを確認
   (`javascript_tool`でDOM操作して実際の保存フローを再現)。
   同じラウンドを`saved=1`無しで再訪すると出ないことを確認。
4. CTAの「バックアップを保存」ボタンを押すと `exportBackup()` が実行され
   (最終バックアップ日時が更新される)、ボタンが「バックアップしました」に
   変わり無効化されることを確認。
5. 累積ダッシュボードでスコア分布カードがバッチ8の絞り込み(ラウンド:すべて等)
   と連動して再集計されることを確認。
6. ホーム画面: 最終バックアップ後に3ラウンド分をシードし、赤背景の強調バナー
   (「前回のバックアップから3ラウンドたまっています。今すぐバックアップして
   ください。」)が表示されることを確認。バックアップ・ラウンドをリセットし、
   「まだバックアップがありません」の従来スタイルの控えめなバナーも表示される
   ことを確認(スタイルが明確に異なることを目視確認)。
7. コンソールエラーなし。

### 判断に迷った点

- 「最終バックアップ以降に保存されたラウンド」の判定には、ラウンドの正確な
  保存時刻を記録するフィールドが存在しない(`round.date`はプレー日のみ)ため、
  `round.date`の0時と最終バックアップの正確な日時を比較する簡略実装にした。
  そのため、**バックアップした当日に新しくラウンドを保存した場合、そのラウンドは
  「バックアップ済み」と誤判定されることがある**(日付粒度の限界)。動作確認で
  実際にこの挙動を確認済み。1日に複数ラウンドを記録するケースは稀だと考え、
  実害は小さいと判断したが、正確な保存時刻をラウンドデータに持たせる方が
  望ましければ別途対応が必要(データ構造の変更を伴うため今回は見送った)。
- スコア分布の配置場所(振り返り・累積ダッシュボードのどのカードの前後か)は
  指示書に明記が無かったため、KPI群とホールタイプ別カードの間に置いた
  (スコアの全体像→内訳の細分化、という自然な流れになるよう判断)。

### スキップした項目

なし。10-1・10-2とも実施。

---

## 全バッチ完了後: Service Workerキャッシュバージョン更新

`service-worker.js` の `CACHE_VERSION` を `golf-log-v4` → `golf-log-v5` に更新
(バッチ6〜10で `css/style.css`・`js/*.js`・`review.html`・`dashboard.html`・
`settings.html` を変更したため)。新規ファイルは追加していないため
`PRECACHE_URLS` の追記は不要。更新後、実ブラウザでService Workerが
`golf-log-v5` として正常に登録され、`PRECACHE_URLS` の23ファイルすべてが
キャッシュされていることを確認した。

---

## 受け入れ確認(fix-instructions-3.md 末尾のチェックリスト)

バッチ6〜10すべて完了したため、9項目すべてを実施した。

1. **9ホールラウンドの除外**: ✅ 9ホール完了ラウンドと18ホール完了ラウンドを
   混在させ、ダッシュボードの既定表示(絞り込み「18Hのみ」)では9ホール
   ラウンドが平均・ベスト・推移グラフから除外され、「ラウンド:すべて」に
   切り替えたときだけ含まれることをコンソール・実画面の両方で確認。
2. **チョロ・ダフリのFWキープ除外**: ✅ ティーショットが `kind="miss"` の
   ホールで `fwKeep === false` になることを確認(バッチ6動作確認時)。
3. **フェアウェイバンカーのサンドセーブ除外**: ✅ ティーショットがフェアウェイ
   バンカーに入り、そこから通常のショットでグリーンに乗せたホールが
   `gsBunker === false` となり、サンドセーブの母数(分母)に入らないことを確認。
4. **OB前進/2打目以降のOB**: ✅ ティーショットのOBで「前進」を選ぶと次打が
   4打目になり、ライが自動でFWになることを実際の入力フロー
   (DOMイベント発火)で確認。2打目以降のOBでは処置選択が出ず、
   打ち直し(+1罰)として即記録されることも確認。
5. **13マスの合計100%**: ✅ 13マスヒートマップの母数(9マス+外周4マスの合計)が
   常にショット数(母数)と一致することをコンソールで確認
   (丸め表示%の合計は四捨五入の影響で±1%程度ずれる場合があるが、
   これは既存の9マスヒートマップにも共通する表示上の丸め誤差であり、
   母数=合計値という定義自体は厳密に一致している)。
6. **絞り込みの連動**: ✅ コース/ティー/ラウンドの絞り込みを変更すると、
   スコア推移・主要指標・ホールタイプ別・コース別・距離帯別パット・
   ヒートマップ・スコア分布のすべてが連動して再集計されることを確認。
7. **KPI設定の反映**: ✅ 設定画面でKPI枠を変更すると、振り返り画面・累積
   ダッシュボードの両方に同じ構成が反映されることを確認。
8. **既存データの互換性(最重要)**: ✅ アップデート前の形式
   (OB左/OB右にdir/adv無し、バンカーショットにgsBunker概念無し、
   設定にkpisフィールド無し)のラウンド・設定データをIndexedDBに直接投入し、
   ホーム・振り返り・累積ダッシュボード・設定・ホール入力(途中再開)の
   全画面をコンソールエラー無く開けること、表示(スコア・KPI・ヒートマップの
   方向振り分け・クラブ設定の保持)が壊れていないことを確認。
9. **オフライン動作**: △(コードレビューによる確認。実環境の機内モードは
   本セッションのブラウザツールでは再現できなかったため、以下で代替確認した)
   - バッチ6〜10で追加・変更したロジックはすべてIndexedDB(`js/db.js`)と
     ローカルの`localStorage`(絞り込み・比較対象・バックアップ日時)のみを
     使用しており、外部ネットワーク呼び出し(fetch/XHR)は一切追加していない
     (元の設計から変更なし)。
   - Service Worker更新後、`PRECACHE_URLS`に列挙された23ファイルすべてが
     `caches.open("golf-log-v5")` に実際にキャッシュされていることを確認した。
   - 以上より、cache-first戦略とオフライン時の入力・保存(IndexedDB)は
     従来どおり機能するはずだが、**実機・実ブラウザでの機内モード確認は
     ユーザー側で一度行うことを推奨する**。

---

## 総括

バッチ6〜10まですべて完了し、それぞれ1コミットずつ作成した(途中で元に
戻した・止めたバッチはない)。ユーザーが確認すべき点は以下の1つ:

- **バッチ9冒頭に記載したCLAUDE.md「v1に入れない範囲」とのKPIカスタマイズUIの
  矛盾**。fix-instructions-3.mdの明示的な指示に従って実装したが、CLAUDE.md/
  SPEC.mdの記述更新が必要かどうかはユーザー確認をお願いしたい。

`git push` は行っていない。ユーザーが内容を確認のうえpushしてください。

---
---

# fix-instructions-4.md 自走セッション(バッチ11〜14)

## バッチ11:オフラインで記録できない不具合の修正(完了)【最優先】

### 原因

指示書の記載どおり。`service-worker.js` の fetch ハンドラが `caches.match(event.request)`
(クエリ文字列まで完全一致)を使っており、アプリがホール遷移に使う
`hole.html?round=xxx&hole=3` のようなクエリ付きURLがプリキャッシュされた
`hole.html` にヒットせず、オフラインで失敗していた。

### 実施内容

- `service-worker.js` の fetch ハンドラを指示書のコードに置き換え。
  - `req.mode === "navigate"` のとき `caches.match(req, {ignoreSearch:true})` で
    クエリを無視してマッチさせ、更に見つからなければ `fetch` にフォールバック、
    オフラインで両方失敗したら `index.html` を返す。
  - navigate以外(js/css/画像等)は従来通りcache-first、フェッチ成功時は
    `res.ok && res.type==="basic"` のときだけキャッシュに書き込む(不透明応答や
    エラー応答をキャッシュしない安全策、指示書のコードそのまま)。
  - 自オリジン以外のリクエストは素通し(`url.origin !== self.location.origin`)。
- `CACHE_VERSION` を `golf-log-v5` → `golf-log-v6` に更新。
- `PRECACHE_URLS` の抜け漏れ確認: `js/` `css/` 配下の実ファイル一覧
  (db.js/clubs.js/stats.js/presetCourses.js/backup.js/sw-register.js/home.js/
  roundStart.js/holeInput.js/review.js/dashboard.js/settings.js/style.css)と
  突き合わせた結果、漏れなし(既存の23件のままで一致)。今後の抜け漏れ防止のため、
  `service-worker.js` 冒頭にファイル追加時は `PRECACHE_URLS` にも追記する旨の
  コメントを追加した。
- オフライン状態の可視化: `js/sw-register.js`(全6画面で読み込み済みの共通スクリプト)
  に `navigator.onLine` を見て `offline`/`online` イベントで画面最上部に
  「オフライン(記録は端末に保存されます)」という帯を出し入れする処理を追加。
  `css/style.css` に `.offline-banner` を追加(`.app` コンテナの外側、bodyの先頭に
  挿入するので全画面共通で効く)。

### 動作確認(指示書の必須手順どおり実施)

`.claude/launch.json` のポートを 8806→8807 に上げてから `preview_start`。

1. `seed-round-1`(18ホール、既存プリセットコース使用、うち3ホール分ショット入りの
   途中ラウンド)をブラウザ上で `js/db.js` を動的importして直接IndexedDBにシード。
2. `index.html` を開いてService Workerを登録・activate待ちしたのち、
   `caches.open("golf-log-v6")` の中身が `PRECACHE_URLS` の23件すべてであることを
   コンソールで確認。
3. **`preview_stop` でサーバプロセス自体を停止**(DevToolsのオフラインチェックボックスでは
   不十分という指示書の警告に従い、実際にプロセスを落とした)。
4. サーバ停止状態のまま以下すべてが表示できることを確認(`get_page_text`で内容も検証):
   - `index.html` → ラウンド一覧(途中ラウンドの表示)が正しく出る。
   - `hole.html?round=seed-round-1&hole=1` → 1番ホールの入力画面が正しく出る。
   - `review.html?round=seed-round-1` → KPI・スコア分布・ヒートマップ等が正しく出る。
   - `dashboard.html` → 完了ラウンドなしのメッセージが正しく出る(エラーにならない)。
   4つとも旧実装なら再現するはずの `net::ERR_FAILED` は発生せず、いずれも
   正常にレンダリングされた。
5. サーバ停止状態のまま `hole.html?round=seed-round-1&hole=4` を開き、9マスグリッドの
   「ナイス」セルをクリックしてティーショットを記録 → 「次のホールへ」ボタンをクリック
   → **オフラインのまま** `hole.html?round=seed-round-1&hole=5` へ遷移し、
   IndexedDB上の `round.holes[3].shots`・`playedHoles` が正しく更新(保存)されている
   ことを確認。オフラインでの「1ホール入力→次ホール遷移→保存」が成立することを確認した。
6. オフライン帯の表示/非表示は、実際のネットワーク遮断ではブラウザ環境の制約上
   `navigator.onLine` を偽装できなかった(この検証環境は実インターネットに繋がって
   おり、開発サーバを止めても `navigator.onLine` はtrueのまま)ため、
   `Object.defineProperty(navigator,"onLine",...)` で値を差し替えたうえで
   `offline`/`online` イベントを発火させて確認: `offline`で帯が出現し文言が
   「オフライン(記録は端末に保存されます)」になること、`online`で帯が消えることを確認。
7. サーバ再起動後、通常のオンライン動作(index.html等)が壊れていないことを確認。

### 判断に迷った点

- オフライン帯を「全画面共通の `js/sw-register.js` にJSで挿入する」方式にした
  (各htmlのheaderに手で仕込む方式も考えたが、`hole.html`だけ`header.hole-header`で
  他画面と構造が違うため、共通スクリプトでbody先頭に差し込む方式のほうが
  6画面すべてに確実に効き、かつ指示範囲(オフライン検知のみ)を超えない)。
- 帯のsafe-area対応(`env(safe-area-inset-top)`)は帯自身に持たせた。結果として
  帯表示中は`header.hero`側のsafe-area分も含めて余白がやや多めになる画面があるが、
  ノッチ端末での表示崩れ防止を優先し、機能上の問題ではないため許容した。

### スキップした項目

なし。

---

## バッチ12:スコアカードとラウンド中のスコア確認(完了)

着手前に `mock/scorecard.html` を実ブラウザ(モバイル幅)で開き、レイアウト・配色・
挙動(前半9・後半9の2ブロック、対Parでの色分け、下部合計、凡例)を確認済み。

### 実施内容

**共通ロジック**

- `js/stats.js` に `computeScorecard(round)` を追加(純粋な集計)。`round.holes`は
  プレー順(index 0 = 1打目のホール)で保持され、各要素の`.number`が実ホール番号
  なので、前半9(index 0-8)・後半9(index 9-17)にブロック分割し、各ブロックの
  先頭ホールの`.number`が9以下かどうかでラベル(OUT/IN)を決めた(IN スタートの
  ラウンドでは前半ブロックが「IN」になる)。現時点ではOUT/IN固定ラベル
  (指示書どおり、バッチ14でコース実名に差し替え予定)。`scorecardCellClass(score,par)`
  で対Parの色分けクラス(s-under/s-par/s-bogey/s-double/s-none)を返す。
- `js/scorecardView.js`(新規・共有モジュール)を追加。`computeScorecard`の結果から
  カード本体・下部合計タイル・凡例のHTML文字列を組み立てる(`scorecardBodyHTML`/
  `scorecardTotalRowHTML`/`scorecardLegendHTML`)。scorecard.html本体とhole.htmlの
  オーバーレイの両方から再利用する(バッチ14でのブロック名差し替えもここ1箇所で済む)。

**12-1(`scorecard.html` / `js/scorecard.js` / `css/style.css`)**

- 新規画面`scorecard.html`。ヘッダーに日付・コース名・ティー・スコア・対Par・
  進捗チップ(「18ホール終了」/「Nホール終了時点」/「未プレー」)、本文に
  前半・後半の4行テーブル(H/Par/スコア/パット)+下部合計+凡例。
- `review.html`のヘッダーに「スコアカード」ボタンを追加(⋯の隣、`.hero-top-actions`)
  し、`scorecard.html?round=<id>`へ遷移するようにした。
- ホーム画面のラウンド一覧からの直接導線(指示書で「あると良い」= 任意扱い)は、
  各行が`<a>`1枚で構成されているため、別の遷移先を追加するには行の構造自体を
  変える必要があり、指示範囲を超える改修になると判断して見送った(判断に迷った点参照)。

**12-2(`hole.html` / `js/holeInput.js` / `css/style.css`)**

- ⋯メニューに「スコアカード」を追加。タップすると`js/scorecardView.js`の同じ
  関数でプレー済みホールまでのスコアカードをオーバーレイ(`.overlay`のボトムシート)
  として表示する。**ページ遷移はしない**(`hole.html`単体の入力状態は
  `holeInput.js`内のメモリ変数`shots`にのみ保持され、DBには次ホール遷移/
  ホールアウト確定まで書き込まれないため、別画面に遷移すると入力中の内容が
  消える。オーバーレイ方式ならその心配がない)。「閉じる」でオーバーレイを
  隠すだけで、入力中のグリッド・ログ・ライ選択等はいっさい再描画されない。
- ヘッダーに現在の累計を常時表示(`.hole-info-col`に`.hole-cum`を追加、例:
  「5H終了 +6」)。プレー済みホールが0件のときは空表示。ホール番号・Parの表示
  レイアウトは崩れないことを確認。

### 動作確認

`.claude/launch.json`のポートを8807→8808に上げてから`preview_start`。

1. モバイル幅(375×812)で18ホール完了ラウンド(モックと同じダミースコア列で
   シード)の`scorecard.html`を表示し、`document.documentElement.scrollHeight`
   (812)が`window.innerHeight`(812)と一致すること、すなわち**スクロールなしで
   18ホール全体が収まる**ことを確認。対Parの色分け(アンダー=青・パー=緑・
   ボギー=橙・ダボ以上=赤)がモックと同じロジックで正しく出ることを確認。
2. INスタートのラウンド(`playOrderNumbers("IN")`で前半に10-18番を持つラウンド)を
   シードし、前半ブロックのラベルが正しく「IN」、後半が「OUT」になり、列見出しが
   実ホール番号(10-18→1-9)で表示されることを確認。
3. 5ホールだけ完了した途中ラウンドで`hole.html?round=..&hole=6`を開き、ヘッダーの
   累計が「5H終了 +6」(スコア27-Par21)と正しいことを確認。6番ホールでティー
   ショットを1つ記録(未保存のメモリ状態)→⋯→スコアカードを開き、1-5番は
   実データ、6番以降は「-」で表示されることを確認→「閉じる」→ログバーに
   記録した1打目がそのまま残っている(`strokeNum`が2打目のまま)ことを確認し、
   **入力中の内容が失われないこと**を実証した。
4. プレー済み0ホールの新規ラウンドで`hole.html`の累計欄が空表示、スコアカードの
   下部合計がすべて「-」になり、コンソールエラーが出ないことを確認。
5. `review.html`のスコアカードボタン→`scorecard.html`遷移→「戻る」ボタンで
   `review.html`に戻ることを確認(`history.back()`)。
6. Service Workerを再登録し、`golf-log-v7`に`scorecard.html`・
   `js/scorecard.js`・`js/scorecardView.js`を含む26ファイルすべてが
   プリキャッシュされることを確認(`CACHE_VERSION`をv6→v7に更新、
   `PRECACHE_URLS`に3ファイル追記)。
7. コンソールエラーなし(全画面)。

### 判断に迷った点

- ホーム画面のラウンド一覧からのスコアカード直接導線は指示書で「あると良い」
  (任意)とされており、既存の`round-card`が1枚の`<a>`で行全体を覆う構造のため、
  別導線を追加するには行の構造(a→div+複数リンク)を変える必要がある。
  指示範囲外の画面を不必要に触るリスクを避け、必須要件(review.htmlからの
  導線・hole.htmlの⋯メニュー)のみ実施し、この任意項目は見送った。
- hole.html側のスコアカードを「別画面への遷移」ではなく「同一ページ内の
  オーバーレイ」にしたのは、`holeInput.js`の入力状態(`shots`配列等)が
  ページ内のJSメモリにしか無く、DB保存は次ホール遷移時までされないため。
  指示書の「入力中の内容は失われないこと」という明示的な条件を満たす
  唯一の安全な方法と判断した。

### スキップした項目

- ホーム画面のラウンド一覧からのスコアカード直接導線(任意項目、上記参照)。

---

## バッチ13:ゴルフ場と9ホールコースのデータモデル(完了)

最もリスクの高いバッチ。着手前に指示書のFacility/Loop/Round/Holeの型定義・
マイグレーション規則・「判断に迷ったとき」節を読み直し、以下の方針で実施した。

### データモデルとマイグレーション(`js/db.js`)

- IndexedDBに `facilities`(ゴルフ場)・`loops`(9ホールコース)の2ストアを追加
  (`DB_VERSION` 1→2、`onupgradeneeded`で追加作成のみ・既存ストアは無変更)。
- `ensureFacilitiesFromCourses()`: 既存`courses`ストアの各Courseを
  `{id:"fac-"+course.id, name}`のFacilityと、`course.id+"-out"`/`"-in"`の
  2つのLoop(pars.slice(0,9)/slice(9,18))に変換して`facilities`/`loops`に追加する。
  同名Facility idが既にあれば何もしない(`ensurePresetCourses`と同じ冪等パターン)。
  **Courseストア自体は削除・変更しない**(指示書どおり)。
- `ensureRoundFacilityFields(round)`: 既存Roundに`facilityId`/`frontLoopId`/
  `backLoopId`、各holeに`loopId`/`loopHole`を補完する。`round.facilityId`が
  既にあれば何もしない(冪等)。`courseId`/`start`は削除しない(非破壊)。
  hole.loopId/loopHoleは**そのホールの実ホール番号だけ**から決まる
  (`number<=9`→`courseId+"-out"`の`number`番、`number>=10`→`"-in"`の
  `number-9`番。`round.start`やfrontLoopIdには依存しない)。指示書の
  マイグレーション規則をそのまま実装した。
- `getFacilities`/`getRounds`/`getRound`は上記2つの補完処理を呼んでから返す
  (`getCourses`が`ensurePresetCourses`を呼ぶのと同じパターン)。
- `getCourse(id)`に`id`がfalsyなら`undefined`を返す防御を追加(下記「判断に
  迷った点」参照)。
- `exportAllData`/`importAllData`(バックアップ)に`facilities`/`loops`を追加
  (version 2)。旧バックアップ(facilities/loops無し)を読み込んでも、
  インポート後の初回アクセスで`ensureFacilitiesFromCourses`が`courses`から
  再生成するので非破壊的に復元できる。

### 新規登録UIとラウンド開始画面

- `js/facilityForm.js`(新規・共有モジュール): ゴルフ場名+9ホールコース
  (コース名+9マスのPar、タップで3/4/5を巡回)を1つずつ追加(最大6つ)、
  または「18ホールとして登録」でOUT/IN固定2コースを一括作成、のどちらかを
  選べる登録フォーム。`settings.html`(ゴルフ場登録カード)・
  `round-start.html`(その場で新規登録)の両方から使い、ロジックの二重管理を避けた。
- `round-start.html`/`js/roundStart.js`を全面書き換え。旧来の「コース(18ホール)
  chip選択+新規コース名入力+OUT/INスタート」を「ゴルフ場を選ぶ(chip選択+
  新規登録ボタン)→前半のコースを選ぶ→後半のコースを選ぶ→ティー」に置き換えた。
  指示書どおり**OUT/INスタート選択UIは廃止**。2コースしかないゴルフ場では
  前半=OUT・後半=IN(相当)を既定選択、1コースのみのゴルフ場では前半=後半=
  そのコースを既定選択、3コース以上は既定を選ばず必ず明示選択させる。
  同じコースを前半・後半の両方に選ぶことも許可(制限なし)。
  ラウンド開始時、`holes[0..8]`=前半Loopの1-9番(`loopId`/`loopHole`/`par`を
  そのLoopから設定)、`holes[9..17]`=後半Loop、で18ホール分のHoleを構築する。
- `settings.html`に「ゴルフ場登録」カードを新設(既存の「コース管理」カードは
  無変更のまま残す。判断に迷った点参照)。登録済みFacility一覧+各Loopの
  Par合計を表示し、「Par修正」から9ホール一覧→該当ホールをタップ→3/4/5を
  選んで「変更する」で確定、という**既存のPar修正と同じ「選ぶ→確定する」
  2段階の方式**を再利用したUIでLoopのParを後から直せるようにした。

### 分析画面(review/dashboard/home/settings)との互換性

- 指示書の「この段階では分析画面を変更しない」に従い、`js/review.js`・
  `js/dashboard.js`・`js/home.js`・既存の「コース管理」機能は**一切変更していない**
  (courseIdベースの集計・コース付け替え・Par変更→コース反映確認などすべて
  従来のまま)。
- そのままだと、新モデルのみで作られたラウンド(courseIdを持たない)が
  `review.js`の`getCourse(round.courseId)`(`Promise.all`内で無条件呼び出し)を
  経由したときに、IndexedDBの`get()`に`undefined`キーを渡して例外になることが
  分かったため、`getCourse(id)`に`id`がfalsyなら即`undefined`を返す1行の防御を
  追加した(下記「判断に迷った点」)。
- `js/roundStart.js`は、選んだゴルフ場が**旧Courseから移行したFacility
  (id が `fac-`で始まる)で、かつ前半・後半がちょうどそのOUT/INのペア**の
  ときだけ、`round.courseId`/`round.start`も一緒に埋めて保存する
  (それ以外の新規ゴルフ場・3コース以上の組み合わせでは`courseId`を持たせない
  =持たせようがない)。これにより、従来どおりの2ループ運用(プリセット3コース・
  18ホールとして登録したゴルフ場)はreview/dashboardの`courseId`ベース集計に
  そのまま乗り、恵庭CCのような3ループ以上のゴルフ場のラウンドだけが
  「コース不明」表示になる(バッチ14で解消予定、これは意図どおりの段階的対応)。
- `js/holeInput.js`の「次のホールへ」保存処理が
  `round.holes[holeNum-1] = {number, par, shots}`と**新しいオブジェクトで
  丸ごと置き換えていた**ため、このままだと保存のたびに`loopId`/`loopHole`が
  消えてしまうバグを発見・修正した(`{...holeData, number, par, shots}`で
  既存フィールドを保ったまま上書きする形に変更)。これは新モデルの正しさに
  直結する必須修正であり、指示範囲(バッチ13の完了条件である「新モデルで
  記録・保存できること」)そのもの。

### 動作確認

`.claude/launch.json`のポートを8808→8809に上げてから`preview_start`。

1. `round-start.html`を開き、プリセット3コース由来の3ゴルフ場が
   chip選択肢に出ることを確認(`getFacilities()`が`ensurePresetCourses`→
   `ensureFacilitiesFromCourses`を正しく連鎖呼び出しし、フレッシュなDBでも
   プリセットFacilityが最初から出ることを確認)。
2. 「札幌リージェントGC 新コース」を選択→前半=OUT・後半=INが既定選択される
   ことを確認→ティー選択→ラウンド開始→`hole.html`で`facilityId`/
   `frontLoopId`/`backLoopId`/`courseId`/`start`、各holeの`loopId`/`loopHole`/
   `par`がすべて期待どおりであることをコンソールで確認。
3. 実際に1番ホールを入力(9マスグリッドでティーショット→PTでカップイン)→
   「次のホールへ」→保存後のIndexedDBで1番ホールの`loopId`/`loopHole`が
   **消えずに残っている**ことを確認(上記バグ修正の検証)。
4. 残り17ホールをDB直接シードで完了させ、`review.html`・`dashboard.html`が
   コンソールエラーなく表示されることを確認。
5. `round-start.html`から「ゴルフ場を新規登録」→「9ホールずつ追加」モードで
   「恵庭カントリー倶楽部」+「摩周コース」(Par37に編集)を作成→
   「9ホールのコースを追加」を2回押して「阿寒コース」「支笏コース」を追加
   (3ループ)→登録→一覧に反映されることを確認。この3ループのゴルフ場を
   選択したときは前半・後半とも**既定選択なし**(明示選択が必要)であることを確認。
6. 摩周→阿寒でラウンド開始→`facilityId`はUUID(`fac-`始まりではない)、
   `courseId`は**設定されない**(旧Courseに対応しないため)ことを確認。
   各holeの`loopId`が実際に選んだLoopの本物のidになっており、`loopHole`が
   1-9で正しく振られていることを確認(この設計により、同じLoopを前半で
   回っても後半で回っても`loopId`は同じになり、バッチ14の集計統合の前提が
   満たされる)。
7. このラウンドをDBシードで完了させ、`review.html`・`dashboard.html`・
   `scorecard.html`・`index.html`・`settings.html`のすべてで
   **コンソールエラーが出ないこと**を確認(`courseId`が無いラウンドの
   グレースフルデグレード)。`index.html`の一覧には「コース不明」と表示され、
   `review.html`の「⋯」からのコース付け替え機能も(旧Course一覧からの選択として)
   クラッシュせず動作することを確認。
8. `settings.html`の「ゴルフ場登録」カードで、摩周コースの3番のParを
   「Par修正」→9ホール一覧→3番をタップ→3を選択→「変更する」で36→36
   (5+4+3+4×6)に変更し、IndexedDBに反映・冪等に再表示されることを確認。
9. **既存データ互換性(最重要)**: `courseId`/`start`のみを持つ旧形式の
   完了ラウンド(`facilityId`等を一切持たない生データ)をIndexedDBに直接
   投入し、`getRound()`を2回呼んで(a)1回目で`facilityId`
   (`fac-preset-sapporo-regent-old`)・`frontLoopId`/`backLoopId`・
   各holeの`loopId`/`loopHole`が正しく補完され、`courseId`/`start`は
   削除されずに残ること、(b)2回目に呼んでも同じ結果になること(冪等)を
   確認。その後`review.html`・`scorecard.html`をコンソールエラーなく
   開けることを確認。
10. バックアップのエクスポート→インポートの往復テスト: `exportAllData()`
    (version 2、facilities 4件・loops 9件・rounds 3件)→`importAllData()`→
    再度`getFacilities()`/`getLoops()`/`getRounds()`で同じ件数・内容が
    復元されることを確認。
11. Service Workerを再登録し、`golf-log-v8`に`js/facilityForm.js`を含む
    27ファイルすべてがプリキャッシュされることを確認
    (`CACHE_VERSION`をv7→v8、`PRECACHE_URLS`に1件追記)。
12. コンソールエラーなし(全画面・全シナリオ)。

### 判断に迷った点

- **`courseId`/`start`互換フィールドを新規ラウンドにも埋めるかどうか**:
  指示書は「既存の courseId/start フィールドは削除せず残す」と
  マイグレーション(既存ラウンドの補完)についてのみ明記しており、
  新規ラウンドについては触れていなかった。一方で指示書は「この段階では
  分析画面を変更しない」とも明記しており、両立させるには新規ラウンドも
  可能な範囲でcourseId互換を持たせる必要があると判断した。「旧Courseに
  1:1対応するFacility(プリセット3コール・『18ホールとして登録』した
  ゴルフ場)で、前半・後半がちょうどそのOUT/INペア」のときだけcourseId/startを
  補うことにし、3ループ以上や任意組み合わせ(恵庭CCのようなケース)は
  courseIdを持たせようがないため持たせない、という設計にした。これは
  「推測で仕様を変える」ではなく、指示書の2つの明示要求(新モデルの
  柔軟性を保つ/分析画面は変更しない)を両立させる必然的な帰結と判断した。
- **`getCourse(id)`のnull防御**: 上記の結果、courseIdを持たないラウンドが
  `review.js`のPromise.all内で無条件に`getCourse(round.courseId)`を呼ぶため、
  IndexedDBの`get(undefined)`が例外を投げてreview.html全体がクラッシュする
  ことが動作確認で判明した。`review.js`自体(分析画面)は変更せず、
  `db.js`の`getCourse`に1行のnullガードを追加するだけで解決したため、
  「分析画面を変更しない」の趣旨(表示ロジック・集計ロジックを変えない)は
  維持しつつ、確認された実クラッシュを防いだ。これは仕様の解釈変更ではなく、
  指示書が想定していなかった組み合わせ(新モデルのみのラウンド×
  変更しない分析画面)から生じる自明なヌルガードの追加と判断した。
- **`holeInput.js`のホールオブジェクト置き換えバグ**: 動作確認の過程で
  発見した実バグ(次ホール保存のたびにloopId/loopHoleが消える)。
  「新モデルで記録・保存できること」というバッチ13の完了条件に直結するため、
  スコープ内の必須修正として対応した。
- **ホーム画面(`index.html`)からのゴルフ場名表示**: `js/home.js`は
  「分析画面」の定義に厳密には含まれないが、指示書に明記が無く、
  変更対象を広げるリスクを避けるため触れなかった。facilityId onlyの
  ラウンドは「コース不明」と表示される(バッチ14で解消予定)。
- **設定画面の「コース管理」と「ゴルフ場登録」の並存**: 新設した
  「ゴルフ場登録」カードとは別に、既存の「コース管理」(旧Course一覧・
  削除機能)カードをそのまま残した。分析画面(dashboard/review)が
  courseIdベースの集計・コース付け替えに`courses`ストアを使い続けるため、
  この管理UIを削除すると分析画面側の導線が失われてしまう。統合はバッチ14で
  分析画面をLoop単位に切り替える際に検討するのが適切と判断した。
- **ラウンド中(hole.html)のPar変更をLoopに同期するか**: 既存の挙動
  (Par変更時に元のCourseにも反映するか確認)は`courseId`がある場合のみ
  従来どおり動作し、facilityId onlyのラウンドでは同期をスキップする
  (静かなno-op、クラッシュなし)。Loop側への同期は指示書の「Parは後から
  でも直せる(既存の仕組みを流用)」を設定画面のLoop編集UIで満たしている
  と判断し、hole.html側の拡張は行わなかった(スコープ超過を避けた)。

### スキップした項目

- ゴルフ場(Facility)・9ホールコース(Loop)の削除機能: 指示書に明記が無く、
  既存の「コース管理」の削除機能(未使用コースのみ削除可)とは削除条件の
  再設計が必要になるため、今回は追加しなかった。
- `index.html`のラウンド一覧でのゴルフ場名表示(現状「コース不明」表示、
  上記参照)。

---

## バッチ14:分析をコース(9ホール)単位に切り替える(完了)

### 実施内容

**`js/stats.js`**

- `dashboardSummary(allRounds, loops, opts)` に変更(第2引数を`courses`から
  `loops`に、`opts.courseId`を`opts.facilityId`+任意の`opts.loopId`に変更)。
  絞り込みは`facilityId`(ラウンドの`facilityId`一致)→`loopId`(そのLoopを
  **前半・後半どちらで回ったラウンドも対象**、`frontLoopId===loopId ||
  backLoopId===loopId`)の順。
- `courseStats`(旧: courseId+`.number`で18ホール分を突き合わせ)を
  `loopStats`(新: facilityId→loopId→9ホール分)に置き換えた。
  各Facilityについて、そのFacilityの各Loopに属する**全ホール
  (`h.loopId===loop.id`で判定、前半・後半どちらの位置で回ったかは問わない)**
  を`h.loopHole`(1-9)で束ね、`holeStats(h).score - h.par`の平均を計算する。
  **これが今回の変更の核心**(同じコースは前半・後半に関係なく合算される)。
- `computeScorecard(round, loopNames)`に第2引数(任意)を追加。
  `loopNames = {loopId: 実コース名}` が渡され、該当ホールブロックの先頭
  ホールの`loopId`がそのマップにあれば実名を、無ければ従来どおり
  実ホール番号からのOUT/IN判定にフォールバックする(既存呼び出し元
  `computeScorecard(round)`は無変更で動く=後方互換)。

**`js/dashboard.js` / `dashboard.html`**

- 絞り込み行を「コース」→「ゴルフ場」+新設「コース(9ホール)」の4項目
  (ゴルフ場/コース/ティー/ラウンド)に変更。`filter-bar`のCSSを3列→2列
  グリッドに変更し2x2で収まるようにした。コース(9ホール)の選択肢は、
  選択中のゴルフ場(未選択なら全ゴルフ場)の中で実際に記録があるLoopだけを
  動的に構築する(ゴルフ場を変えると選び直しになり、無効な選択は自動で
  「すべて」にリセットされる)。
- 「コース別・ホール別」カードを、ゴルフ場タブ(`facilityTabRow`)→
  コース(9ホール)タブ(`loopTabRow`、選択中のゴルフ場のLoopのみ表示)→
  9マスのホールタイル(旧18マス)の2段階ナビゲーションに作り直した
  (`.hole-grid`のCSSも6列→3列に変更し、9マスがきれいな3x3で収まるように
  した)。要対策ホールの読み解き文はLoop単位(9ホール中)の文言に変更。

**`js/scorecardView.js` / `js/scorecard.js` / `js/holeInput.js`**

- `scorecardBodyHTML(round, loopNames)`に第2引数(任意)を追加し
  `computeScorecard`にそのまま渡すだけにした。
- `scorecard.js`(スコアカード画面本体): `getFacility(round.facilityId)`・
  `getLoop(round.frontLoopId)`・`getLoop(round.backLoopId)`を取得して
  `loopNames`マップを作り、ブロック見出しに実コース名(例:「摩周コース」)が
  出るようにした。ヘッダーの施設名表示も、旧`courseId`ベースの表示から
  `facility.name`優先(無ければ旧`course.name`にフォールバック)に変更。
- `holeInput.js`(⋯からのスコアカードオーバーレイ): 起動時に前半・後半の
  Loop名を一度だけ取得しておき、オーバーレイを開くたびに同じ`loopNames`を
  渡す(実コース名が表示される)。

**`js/review.js`**

- ヘッダーの表示を「ゴルフ場名 前半コース→後半コース・ティー」
  (例:`恵庭カントリー倶楽部 摩周コース→阿寒コース・白ティー`)に変更する
  `facilityHeaderText()`を追加。同じコースを前半・後半とも回った場合は
  矢印を省いて「ゴルフ場名 コース名・ティー」にする。`facility`/
  `frontLoop`/`backLoop`が引けない(想定外の未移行データ等)場合は
  旧来の`course.name`表示にフォールバックする。**コース付け替え機能
  (「⋯」ではなく`courseNameBtn`タップで開く旧Course一覧からの選択)自体は
  無変更**(表示文言だけを差し替えた。判断に迷った点を参照)。

**`service-worker.js`**: `CACHE_VERSION`をv8→v9に更新(新規ファイルは
追加していないため`PRECACHE_URLS`の追記は不要)。

### 動作確認

`.claude/launch.json`のポートを8809→8810に上げてから`preview_start`。

1. **今回の核心(受け入れ確認5に対応)**: 「恵庭カントリー倶楽部」
   (摩周/阿寒/支笏の3Loop)をIndexedDBに直接投入し、
   「ラウンド1: 摩周(前半・1番=スコア6=Par4+2)→阿寒(後半)」
   「ラウンド2: 阿寒(前半)→摩周(後半・1番=スコア4=Par通り)」の2ラウンドを
   記録。ダッシュボードのコース別・ホール別カードで「恵庭カントリー倶楽部」
   →「摩周コース」を選択すると、両ロープとも`×2R`と表示され、
   摩周コース1番のタイルが**+1.0**((+2 + 0) ÷ 2)になっている
   ことを確認。前半で回ったか後半で回ったかに関係なく合算されていることを
   実データで確認できた(最重要項目)。
2. 絞り込み行の「ゴルフ場」を選ぶと「コース(9ホール)」の選択肢が
   選択したゴルフ場のLoopだけに絞り込まれることを確認。「コース(9ホール)」
   に「摩周コース」を選ぶと、摩周を前半・後半どちらで回ったラウンドも
   両方カウントされる(該当2ラウンドのまま)ことを確認。
3. 旧形式(`courseId`のみ・Facility/Loop未移行)の完了ラウンドをIndexedDBに
   直接投入し、`getRounds()`経由のマイグレーション後、ダッシュボードの
   ゴルフ場タブに「札幌リージェントGC 新コース」が追加され、
   コース(9ホール)タブが「OUT」「IN」として現れ、9ホールぶんのタイルが
   正しい値で表示されることを確認(既存データ互換性、受け入れ確認6)。
4. `scorecard.html`・`hole.html`の⋯スコアカードオーバーレイの両方で、
   ブロック見出しが実際のLoop名(「摩周コース」「阿寒コース」)になって
   いることを確認。旧形式ラウンドでは従来どおり「OUT」「IN」表示に
   フォールバックすることを確認。
5. `review.html`のヘッダーが新モデルのラウンドで
   「恵庭カントリー倶楽部 摩周コース→阿寒コース・白ティー」、
   旧形式ラウンドで「札幌リージェントGC 新コース OUT→IN・赤ティー」と
   正しく表示されることを確認。
6. **オフライン受け入れ確認の再検証(最重要・バッチ13でround-start.jsを
   全面書き換えたため再実施)**: サーバプロセスを停止した状態で
   `index.html`→`round-start.html`(ゴルフ場「恵庭カントリー倶楽部」選択→
   前半「支笏コース」・後半「阿寒コース」を明示選択→ラウンド開始)→
   `hole.html`(実際に1番ホールをグリッド操作で入力し「次のホールへ」で
   2番へ遷移)→残り17ホールをIndexedDB直接操作で入力→
   `review.html?saved=1`まで、すべてサーバ停止状態のまま
   コンソールエラーなく完了できることを確認した(バックアップCTA表示も
   確認)。ヘッダーの表記もオフラインのまま正しく
   「恵庭カントリー倶楽部 支笏コース→阿寒コース・赤ティー」と表示された。
7. Service Workerを再登録し、`golf-log-v9`が27ファイル(新規ファイル無し)
   すべてプリキャッシュされることを確認。
8. コンソールエラーなし(全画面・全シナリオ)。

### 判断に迷った点

- **`review.js`のコース付け替え機能を新モデル対応させるか**: 「⋯」から
  ではなく`courseNameBtn`タップで開く旧来のコース付け替え(`courseId`を
  別の`courses`ストアのレコードに変更する機能)は、指示書のバッチ14の
  範囲(「振り返り画面のヘッダーの**表示**も…表記にする」)には含まれて
  いないと判断し、機能自体には手を入れず表示文言だけ変更した。Facility/Loop
  ベースの付け替えUIへの刷新は指示書に明記が無く、今回のスコープ外と判断した。
- **`index.html`のラウンド一覧のコース名表示**: バッチ14の指示の列挙
  (ダッシュボードのコース別・スコアカード・振り返りヘッダー)に含まれて
  いないため、バッチ13から引き続き「コース不明」表示のまま変更しなかった
  (facilityIdのみのラウンドが対象)。

### スキップした項目

- `index.html`のラウンド一覧でのゴルフ場名表示(上記参照、バッチ13から
  継続して未対応)。
- `review.js`のコース付け替え機能のFacility/Loop対応化(上記参照)。

---

## 全バッチ完了後: 受け入れ確認(fix-instructions-4.md 末尾のチェックリスト)

バッチ11〜14すべて完了したため、7項目すべてを実施した。

1. **サーバ停止状態での開始→18ホール入力→保存→振り返り**: ✅
   バッチ14の動作確認6で、新しいゴルフ場/コース選択のラウンド開始画面から
   実際にサーバプロセスを停止した状態で一通り実施し、コンソールエラーなく
   完了できることを確認(バッチ11時点でも旧roundStart.jsで同様に確認済み)。
2. **オフライン表示の出現/消滅**: ✅ バッチ11で`navigator.onLine`を偽装した
   `offline`/`online`イベントで確認済み。バッチ12〜14では`js/sw-register.js`
   自体は変更していないため再検証は不要と判断し、上記6のオフライン一気通貫
   確認でページ遷移・入力・保存が正常に機能すること自体は重ねて確認した。
3. **スコアカードが1画面(スクロールなし)に18ホール**: ✅ バッチ12の動作確認1で
   `scrollHeight === innerHeight`(812=812)を確認済み。バッチ14でブロック見出しが
   実コース名に変わったが、行数・レイアウト構造は変えていないため崩れは無い
   (バッチ14動作確認4で目視・構造確認済み)。
4. **⋯からスコアカードを開閉しても入力中の内容が失われない**: ✅ バッチ12の
   動作確認3で確認済み(ページ遷移せずオーバーレイのみで完結する実装のため、
   バッチ14でのローカル名解決追加も含め、この性質は変わらない)。
5. **同じコースの合算(3コース以上のゴルフ場)**: ✅ バッチ14の動作確認1で、
   摩周コースを前半・後半それぞれで回った2ラウンドの平均が正しく
   合算((+2+0)÷2=+1.0)されることを実データで確認(最重要)。
6. **既存データの互換性(全画面)**: ✅ バッチ11〜14の各動作確認で、
   `courseId`/`start`のみを持つ旧形式ラウンド・`dir`/`adv`/`gsBunker`/`kpis`
   未対応の更に古い形式のデータを含め、index/hole/review/dashboard/
   scorecard/settingsの全画面をコンソールエラーなく開けることを都度確認した。
7. **バックアップのエクスポート→インポートで新モデルが完全復元**: ✅
   バッチ13の動作確認10で、facilities 4件・loops 9件・rounds 3件を含む
   エクスポート→インポートの往復で内容が完全に一致することを確認済み。

---

## 総括(バッチ11〜14)

バッチ11〜14まですべて完了し、それぞれ1コミットずつ作成した(途中で元に
戻した・止めたバッチはない)。ユーザーが確認・判断すべき点は以下のとおり:

- **バッチ13で導入した「courseId/start互換フィールド」の設計方針**:
  旧Courseに1:1対応するFacility(プリセット3コース・「18ホールとして登録」
  したゴルフ場)で前半・後半がちょうどそのOUT/INペアのときだけ、新規
  ラウンドにも`courseId`/`start`を補って保存している(分析画面を段階的に
  移行するための橋渡し措置)。バッチ14で分析のコア部分はLoop単位に
  切り替えたが、`review.js`のコース付け替え機能・`index.html`のコース名
  表示はまだ`courseId`ベースのまま(恵庭CCのような3コース以上のゴルフ場の
  ラウンドでは「コース不明」表示・コース付け替え機能が使えない)。
  今後これらも作り直すかどうかはユーザー判断が必要。
- **ゴルフ場・コースの削除機能が無い**: バッチ13で追加登録はできるが、
  削除は指示書に明記が無く実装していない。誤登録した場合は現状復旧手段が
  無い(IndexedDBの直接操作以外)。

`git push`は行っていない。ユーザーが内容を確認のうえpushしてください。
