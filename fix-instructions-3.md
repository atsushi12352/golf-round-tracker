# 修正指示書 その3(Claude Code用)

## ⚠️ このファイルの使い方(最初に読むこと)

このファイルには**バッチ6〜10の5つの作業**が入っている。
**ユーザーは不在。質問には答えられない前提で、最後まで自走すること。**

### 進め方

1. **バッチ6 → 7 → 8 → 9 → 10 の順に、すべて実施する。**順番は変えない
   (6の集計バグを直す前に9の指標追加をやると、誤った定義のまま指標が増えてしまう)。
2. **1バッチ = 1コミット。**各バッチが終わるたびに動作確認してからコミットする。
   途中で問題が起きたときにバッチ単位で戻せるようにするため。
   コミットメッセージは `バッチ7: 大ミスの方向記録と13マスヒートマップ` のように
   バッチ番号で始めること。
3. **`git push` はしない。**ユーザーが戻ってから内容を確認して自分で push する。
4. 全バッチ終了後、末尾の「受け入れ確認」を実施し、結果を記録する。

### 判断に迷ったとき(重要)

ユーザーが不在なので、**勝手な推測で進めない**。次のいずれかに該当したら、
**そのバッチの作業を元に戻し(直前のコミットに戻す)、`WORKLOG.md` に理由を書いて、
そこで作業を終了する。以降のバッチには進まない。**

- 仕様が曖昧で、解釈によって結果が大きく変わる
- 動作確認が通らず、原因が特定できない
- 指示された変更が既存の保存データを壊す・読めなくする可能性がある
- モックと指示書の内容が食い違っている

「とりあえず動くように回避策を入れる」「仕様を自分の判断で変える」は禁止。
止まって報告するほうが、間違ったまま5バッチ分積み上がるよりはるかに良い。

### 作業ログ

リポジトリ直下に `WORKLOG.md` を作り、各バッチの終了時に追記していく。内容は:
- バッチ番号と実施した内容の要約
- 動作確認として何を試し、どういう結果だったか
- 判断に迷って自分で決めたこと(あれば)、気づいた懸念点
- スキップした項目があればその理由

ユーザーはこのファイルを見て何が起きたかを把握するので、
**あとから読んで分かるように書くこと。**

### 参考モック

このリポジトリの `mock/` にある。**挙動の正解サンプル**なので、
関係するバッチに着手する前に必ずブラウザで開いて確認すること。

- `mock/heatmap13.html` … 13マスヒートマップの見た目とロジック
- `mock/input13.html` … 大ミスの方向入力フロー、OBの処置選択(打ち直し/前進)、ラベル削除後の入力画面

---

## バッチ6:集計バグの修正3件

いずれも `js/stats.js` の集計定義の問題。表示側の変更は最小限で済むはず。

### 6-1. 9ホールラウンドが18ホールと同列に集計されている

現象:「保存して終了」で9ホールだけ保存したラウンドが、累積ダッシュボードで18ホール
ラウンドと同じ重みで集計されている。結果、ベストスコアが9ホールの記録になったり、
平均スコアが下振れしたり、パット/R・3パット/R・ペナルティ/Rが半分になる。
(`dashboardSummary` が `r.complete` だけで絞っており、プレーホール数を見ていない)

対応:
- 累積ダッシュボードの集計対象を、既定で**18ホール完了したラウンドのみ**にする。
- ただしバッチ8で追加する絞り込み行に「ラウンド:18Hのみ / すべて」を用意し、
  既定を「18Hのみ」にする。ユーザーが明示的に切り替えたときだけ9Hも含める。
- 9ホールのラウンドも振り返り画面では従来どおり閲覧できること(削除しない)。

### 6-2. チョロ・ダフリのティーショットがフェアウェイキープ扱い

現象:`holeStats` の `fwKeep` が結果文字列に「左」「右」が含まれるかだけを見ているため、
チョロ・ダフリ(kind="miss")のティーショットは左右どちらも含まず**キープ扱い**になる。
30ヤードのトップがフェアウェイキープになってしまう。

対応:ティーショットの kind が "miss" のときも fwKeep = false とする
(現状 "ob" と "penalty" は除外済みなので、"miss" を加えるだけ)。

### 6-3. サンドセーブがフェアウェイバンカーも母数に数えている

現象:`sandSave` が「そのホールのどこかにバンカーのライがあったか」で判定しているため、
ティーショットでフェアウェイバンカーに入れたホールも母数に入る。

対応:**グリーンサイドのバンカーのみ**を母数にする。判定は
「バンカーのライから打ったショットの**次のショットがパット**(またはそのバンカー
ショット自体がカップイン)であるとき、そのバンカーはグリーンサイドとみなす」。
サンドセーブ成立の条件(スコア ≤ par)は従来どおり。

テスト:ティーショットでバンカー→その後グリーンに乗せてパー、というホールが
サンドセーブの母数に入らないこと。

---

## バッチ7:大ミスの方向記録と13マスヒートマップ

`mock/input13.html` と `mock/heatmap13.html` が正解サンプル。**先に両方ブラウザで開いて挙動を確認すること。**

### 7-1. 入力:OB・ペナルティに方向を持たせる

- 特殊ボタンを **OB / ペナ(赤杭・池) / トップ・チョロ / チップイン の4つ**にする
  (現在の「OB左」「OB右」は廃止し「OB」1つに統合)。
- OB または ペナ を押すと、**9グリッドがそのまま方向選択パネルに変わる**:
  上中央=「奥」、左中央=「左」、右中央=「右」、下中央=「手前」の4マスだけが有効になり
  赤系で強調、残り5マスは薄く無効化。ガイド文は「OBはどっちに?」。
  特殊ボタン行は「やめる」ボタンに差し替わり、押すと通常モードに戻る。
- 方向を選ぶと1打記録して通常モードに復帰。モックと同じ挙動にすること。
- データ:Shot に `dir` を追加。`"左" | "右" | "奥" | "手前"`。
  kind="ob" と kind="penalty" のときのみ必須。result は `"OB右"` `"ペナ手前"` のような文字列。
- OBとペナルティは**データ上は今までどおり別kindのまま**にする(ライの自動推測が
  OB=打ち直しで同じライ、ペナ=ドロップでラフ、と異なるため)。

### 7-1b. OBの処置(打ち直し / 前進)を選べるようにする

背景:ゴルフ規則の正式な処置は「ストロークと距離」= **1罰打で元の位置から打ち直し**。
一方、多くのコースがローカルルールとして **2罰打で前進**(日本の特設ティー=プレイング4、
またはモデルローカルルールE-5)を採用している。どちらを使うかはコース・同伴者次第なので、
アプリが決め打ちせずプレーヤーに選ばせる。

- **OBの方向を選んだあと**、続けて「打ち直し(+1罰・元の位置から)」「前進(+2罰・特設ティー等)」
  の2択を表示する。選ぶと記録して通常モードに戻る。「やめる」で取り消せること。
- **この2択を出すのは、そのOBがティーショット(ライ="ティー")だったときだけ。**
  2打目以降のOBは前進の選択肢がないので、方向を選んだ時点で「打ち直し(+1罰)」として
  即記録する(タップ数を増やさない)。
- データ:Shot に `adv`(boolean)を追加。前進なら true。
- **罰打の計算**:kind="ob" は `adv ? 2 : 1` 打罰。kind="penalty"(赤杭・池)は常に1打罰。
  `holeStats` の `pen` の計算をこれに合わせて修正すること
  (現状は ob も penalty も一律1打罰で数えている)。
- **ライの自動推測**:OBのあとは `adv ? "FW" : 元のライ` とする
  (前進=特設ティーはフェアウェイ上、打ち直し=元の位置)。現状は一律で元のライに戻している。
- 表示:履歴チップに「1W OB右(前進)」「7I OB左(打直)」のように処置を併記する。
  記録直後のトーストにも「前進 +2打罰」「打ち直し +1打罰」と出す。
- ヒートマップ上の扱いは打ち直しでも前進でも同じ(方向どおり外周のマスに入る)。
- スコアロス要因の「OB・ペナルティ 1発 = −2打」換算は**変更しない**
  (打ち直しでも前進でも実質的な損失はおおよそ2打のため)。
- 既存データ(adv 未設定)は `adv=false`(打ち直し)として扱う。

**既存データのマイグレーション**:
- result が "OB左" のものは dir="左"、"OB右" は dir="右" を補完。
- result が "ペナルティ" のもの(方向未記録)は dir=null のままとし、ヒートマップでは
  下段「手前へ大ミス」に合流させる。振り返り画面の「修正」で後から直せる状態でよい。

### 7-2. ヒートマップを13マスにする

`mock/heatmap13.html` のレイアウト・色・集計に合わせる。

- 構成:内側に従来の9マス、その外周4マスに大ミスを配置。
  - 上バー「奥へ大ミス」= kind ob/penalty の dir="奥"
  - 下バー「手前へ大ミス」= dir="手前" + kind="miss"(トップ・チョロ)+ dir未設定のペナルティ
  - 左バー「左へ大ミス」= dir="左"(縦にぶち抜き)
  - 右バー「右へ大ミス」= dir="右"(縦にぶち抜き)
- **母数は13マスの合計**(パット以外の全ショットから kind="in" を除いたもの)。
  各マスの%は13マス合計に対する割合で、合計100%になること。
  これによりナイス率が「大ミスも母数に入れた数字」になる(仕様変更として意図的)。
- 色:内側9マスは従来の緑ランプ、外周4マスは赤茶ランプ(モックの `RED` 配列)。
  それぞれ自分のグループ内の最大値で正規化する。
- **軸ラベル(左の「オーバー/ジャスト/ショート」、下の「左/中/右」)は削除**。
  外周バーの文言が方向を示すので不要。
- 読み解き文:ナイス率、ミスで最も多いマス、大ミスの割合と内訳(左◯・右◯・奥◯・手前◯)。
  モックの文面に合わせる。助言は入れない(現状方針どおり)。
- パットのヒートマップは**従来の9マスのまま**(パットに大ミスの概念がないため)。
- 振り返り画面・累積ダッシュボードの両方に反映する。

### 7-3. ラベルの削除

- 入力画面のライ選択の左にある「ライ」の文字を削除。
- 入力画面のパット距離選択の左にある「距離」の文字を削除。
- チップの文言だけで何の選択か分かるため。

---

## バッチ8:ダッシュボードの絞り込み

- 累積ダッシュボードの最上部(ヘッダーの下)に絞り込み行を1本置く。
- 項目は3つ:
  - **コース**:すべて / 登録済みコース個別
  - **ティー**:すべて / 赤 / 白 / 青 / 黒(記録に存在するものだけ出す)
  - **ラウンド**:18Hのみ(既定) / すべて ← バッチ6-1で使う
- 選択するとダッシュボード全体(ヘッダーの平均・ベスト、推移グラフ、KPI、
  ホールタイプ別、コース別、ヒートマップ)がすべて連動して絞り込まれる。
- 各カードに該当ラウンド数を必ず表示する(例:「該当5ラウンド」)。
  絞り込みで母数が減ったことがユーザーに見えるようにするため。
- 該当0ラウンドのときは「条件に合うラウンドがありません」と表示し、
  グラフや空のタイルを描かないこと。
- 選択状態は保存し、次回開いたときも維持する。

---

## バッチ9:KPIタイルの選択式と比較対象

### 9-1. KPIタイルを設定から選べるようにする

- 設定画面に「表示する指標」を追加。**9枠**に、下記の候補から好きなものを割り当てる。
  並べ替えもできること。既定は現在の9個(★印)。
- 振り返り画面と累積ダッシュボードで同じ設定を使う。
- すべて既存の記録から追加入力なしで計算できる。累積画面ではラウンド当たりの平均、
  振り返り画面ではそのラウンドの値を表示する。

**スコア系**
- ★ ペナルティ打数(OB+ペナルティ)
- ダボ以上:スコアが par+2 以上のホール数
- パーセーブ率:スコア ≤ par のホール率
- バーディ数:スコア ≤ par−1 のホール数
- 前半/後半スコア差:プレー順の前9ホール合計 − 後9ホール合計

**ティーショット系**
- ★ FWキープ率:par4以上のホールで、ティーショットが ob/penalty/miss でなく、結果に左右を含まない
- ティーショット無事率:par4以上のホールで、ティーショットが ob/penalty/miss でなかった率
- FWキープ時とミス時のスコア差:キープしたホールの平均スコア − 外したホールの平均スコア

**ショット系**
- ★ パーオン率(GIR)
- ★ ボギーオン率
- ★ ナイス率(パット以外の全ショット中 kind="nice" の割合。分母は13マス母数と揃える)
- クラブ別ナイス率:ドライバー / アイアン / ウェッジ の3種を個別のタイルとして選べる
- ラフからのグリーンオン率:ライ="ラフ" から打ったショットのうち、次がパットだった割合
- パー3のパーオン率

**ショートゲーム系**
- ★ 寄せワン(スクランブリング)
- ★ サンドセーブ(バッチ6-3の新定義)
- トラブル脱出率:ライ="林" または "バンカー" から打ったショットのうち、
  次のライが FW・グリーンだった割合

**パット系**
- ★ パット合計(累積ではパット/R)
- ★ 3パット回数
- 1パット率:1パットで上がったホール率
- 2パット以内率
- **パーオンホールの平均パット数**:GIR成立ホールのみのパット平均(寄せワンで拾った
  パットが混ざらないので純粋なパット力が見える)
- 1m以内の成功率:dist="1m以内" のパットが1打で入った率
- 10m以上からの3パット率:1打目が dist="ロング" だったホールのうち3パット以上の率

### 9-2. 比較対象を選べるようにする

- 振り返り画面のKPIカードに、比較対象を選ぶセレクタを置く。候補:
  - **直近5ラウンドの平均(既定)**
  - 全期間の平均
  - 自己ベストのラウンド
  - 同じコースでの平均
  - 前回のラウンド
- 各KPIタイルに小さく併記する(例:「パット34(直近5R平均 36.2)」)。
- 良い方向の差は緑、悪い方向は赤(累積ダッシュボードの▲▼と同じ配色ルール)。
- 比較対象が存在しない・母数不足のときは併記を省略する(エラーにしない)。
- 選択は保存する。
- 累積ダッシュボードの「直近3R vs その前3R」は現状のまま変更しない。

---

## バッチ10:スコア分布帯とバックアップ導線

### 10-1. スコア分布の積み上げ帯

- 振り返り画面と累積ダッシュボードの両方に、横1本の積み上げ帯を追加する。
- 区分:イーグル以下 / バーディ / パー / ボギー / ダボ / トリプル以上。
- 各区分の割合とホール数を表示。ダボ以上が一目で分かることが目的。
- 色は1系統の濃淡(良い側が薄く、悪い側が濃い)で、区分名は直接ラベルする。

### 10-2. バックアップ導線

- ラウンド保存直後の振り返り画面に「バックアップを保存」ボタンを出す
  (設定画面のエクスポートと同じ処理でよい)。記録した直後が一番忘れにくいため。
- 最終バックアップ以降に保存されたラウンドが**3つ以上**たまったら、
  ホーム画面の既存の警告を強調表示にする(色・文言を強める)。
- 最終バックアップ日時は既存の仕組みがあればそれを使う。

---

## 共通条件(全バッチ共通・必ず守ること)

- 作業開始前に、作業ツリーがクリーンであることを確認する。未コミットの変更があれば先にコミットする。
- 各バッチで、**指示した範囲以外の画面・データ構造・集計は変更しない。**
- **データのマイグレーションは必ず非破壊・冪等にする。**既存の記録を書き換えるのではなく、
  不足しているフィールドを補う形にする。同じマイグレーションが2回走っても壊れないこと。
  既存データを削除・上書きしなければ実現できない変更が必要になった場合は、実装せずに
  `WORKLOG.md` に書いて止まる。
- 各バッチの修正後は、該当機能の操作手順を自分で動作確認してからコミットする。
  動作確認には、既存の保存データを読み込んだ状態で表示が壊れていないことの確認を必ず含める。
- Service Worker のキャッシュバージョンは、全バッチ完了後に一度だけ更新すればよい。

---

## 受け入れ確認(全バッチ完了後に実施し、結果を WORKLOG.md に記録)

1. 9ホールで保存したラウンドが、累積ダッシュボードの平均・ベスト・推移グラフに
   含まれていないこと(絞り込みで「すべて」にしたときだけ含まれること)。
2. チョロ・ダフリのティーショットがフェアウェイキープに数えられないこと。
3. ティーショットでフェアウェイバンカーに入れたホールが、サンドセーブの母数に入らないこと。
4. ティーショットのOBで「前進」を選ぶと、次が4打目になり、ライが自動で FW になること。
   2打目以降のOBでは処置の選択が出ず、打ち直し(+1罰)として記録されること。
5. ヒートマップの13マスの割合の合計が100%になること。
6. 絞り込みを変えると、ダッシュボード上のすべてのカードが連動して変わること。
7. 設定でKPIタイルを差し替えると、振り返り画面と累積ダッシュボードの両方に反映されること。
8. **アップデート前に保存されていたラウンドが、すべて開けて表示が壊れていないこと。**
   (最重要。既存データの互換性が壊れていないかの確認)
9. 機内モード相当のオフライン状態でも従来どおり入力・保存できること。
