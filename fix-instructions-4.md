# 修正指示書 その4(Claude Code用)

## ⚠️ このファイルの使い方(最初に読むこと)

このファイルには**バッチ11〜14の4つの作業**が入っている。

### 進め方

1. **バッチ11 → 12 → 13 → 14 の順に、すべて実施する。**順番は変えない
   (11はゴルフ場で記録できなかった不具合の修正なので最優先。
    13でデータモデルを変えるため、12までを安定させてから着手する)。
2. **1バッチ = 1コミット。**各バッチが終わるたびに動作確認してからコミットする。
   コミットメッセージは `バッチ11: オフライン不具合の修正` のようにバッチ番号で始める。
3. **`git push` はしない。**
4. 各バッチの終了時に `WORKLOG.md` に追記する(実施内容・動作確認の結果・
   自分で判断したこと・懸念点)。
5. 全バッチ終了後、末尾の「受け入れ確認」を実施し、結果を WORKLOG.md に記録する。

### 判断に迷ったとき

**勝手な推測で進めない。**仕様が曖昧で解釈により結果が大きく変わる、動作確認が通らず
原因が特定できない、既存の保存データを壊す可能性がある、モックと指示書が食い違う——
このいずれかに該当したら、**そのバッチを直前のコミットまで戻し、`WORKLOG.md` に理由を
書いて終了する。以降のバッチには進まない。**
「とりあえず動くように回避策を入れる」「仕様を自分の判断で変える」は禁止。

### 参考モック

- `mock/scorecard.html` … スコアカードのレイアウト(バッチ12・14の正解サンプル)

---

## バッチ11:オフラインで記録できない不具合の修正【最優先】

### 症状

ゴルフ場の電波が届かない場所でアプリを開くとエラーになり、記録ができなかった。

### 原因(調査・再現済み)

`service-worker.js` の fetch ハンドラが `caches.match(event.request)` を使っている。
Cache API の照合は**クエリ文字列まで含めた完全一致**なので、プリキャッシュされた
`hole.html` は `hole.html?round=xxx&hole=3` にヒットしない。

アプリはホール間の遷移を `location.href = "hole.html?round=" + roundId + "&hole=" + n`
のようにクエリ付きで行っているため、**オフラインではラウンド開始も次ホールへの遷移も
すべて失敗する**。さらに `.catch(() => cached)` の `cached` はこの時点で undefined
なので、`respondWith(undefined)` となりネットワークエラー画面になる。

ローカルにHTTPサーバを立ててService Workerを登録し、**サーバプロセスを落とした
完全オフライン状態**で検証した結果:

```
index.html (クエリなし)          -> OK 200
hole.html?round=abc&hole=1       -> FAIL net::ERR_FAILED
review.html?round=..&saved=1     -> FAIL net::ERR_FAILED
dashboard.html (クエリなし)      -> FAIL timeout
```

### 修正

`service-worker.js` の fetch ハンドラを下記に置き換える。同じ検証環境で
**4つとも OK 200 になることを確認済み**。`CACHE_VERSION` は `golf-log-v6` に上げること。

```js
self.addEventListener("fetch", (event) => {
  const req = event.request;
  if (req.method !== "GET") return;

  const url = new URL(req.url);
  if (url.origin !== self.location.origin) return; // 自サイト以外は素通し

  // ページ遷移(navigate)は必ずキャッシュで応答できるようにする。
  // アプリは hole.html?round=...&hole=3 のようにクエリ付きで遷移するため、
  // ignoreSearch を付けないとキャッシュにヒットせずオフラインで失敗する。
  if (req.mode === "navigate") {
    event.respondWith(
      caches.match(req, { ignoreSearch: true })
        .then((cached) => cached || fetch(req))
        .catch(() => caches.match("index.html"))
    );
    return;
  }

  event.respondWith(
    caches.match(req).then((cached) => {
      if (cached) return cached;
      return fetch(req)
        .then((res) => {
          if (res && res.ok && res.type === "basic") {
            const copy = res.clone();
            caches.open(CACHE_VERSION).then((cache) => cache.put(req, copy));
          }
          return res;
        })
        .catch(() => caches.match(req, { ignoreSearch: true }));
    })
  );
});
```

### あわせて対応すること

- **`PRECACHE_URLS` の抜け漏れ確認**:`js/` `css/` 配下の実ファイル一覧と
  `PRECACHE_URLS` を突き合わせ、列挙漏れがあれば追加する。
  今後ファイルを増やしたときに漏れないよう、抜けを検知する手段(コメントでの注意書きでもよい)を残す。
- **オフライン状態の可視化**:`navigator.onLine === false` のとき、画面上部に
  「オフライン(記録は端末に保存されます)」という控えめな帯を出す。
  ゴルフ場で不安にならないようにするため。オンラインに戻ったら消す。
- 記録・保存は元々 IndexedDB のみでネットワークを使っていないので、**保存処理側の変更は不要**。

### 動作確認(必須)

ローカルサーバでアプリを開いてService Workerを登録したあと、**サーバを停止した状態**で
以下がすべて開けること。ブラウザのDevToolsの「オフライン」チェックだけでは
Service Worker経由の通信をすり抜ける場合があるため、**サーバ自体を止めて確認すること**。

1. `index.html`
2. `hole.html?round=<既存のラウンドID>&hole=1`
3. `review.html?round=<既存のラウンドID>`
4. `dashboard.html`
5. オフラインのまま1ホール分の入力→次のホールへ遷移→保存ができること

---

## バッチ12:スコアカードとラウンド中のスコア確認

`mock/scorecard.html` が正解サンプル。**先にブラウザで開いて確認すること。**

### 12-1. スコアカード画面

- 18ホールを**スクロールなしの1画面**に収める。前半9・後半9の2ブロック構成。
- 各ブロックは4行:**ホール番号 / Par / 対Par / パット**、右端に「計」列。
- **「対Par」の行はマス内に実打数ではなくParに対する差を表示する。**
  ボギーなら `+1`、ダボなら `+2`、パーなら `0`、バーディなら `-1`。
  「計」列にはその9ホールの差の合計(例 `+11`)を出す。
- **実打数はブロック見出しに出す**(例:`摩周コース   47打 +11 / Par 36`)。
  18ホールぶんの実打数は下部のトータルに出る。
- ブロック見出しのコース名は、現時点では「OUT」「IN」でよい
  (バッチ14で実際のコース名に差し替える)。
- 対Parのマスは値で色分けする:
  バーディ以下=青系、パー=緑系、ボギー=橙系、ダボ以上=赤系。凡例を下に置く。
- 下部にトータル(スコア / パット / 対Par / ペナルティ)。
- 未プレーのホールは「-」で表示する(ラウンド中の表示に使う)。
- 振り返り画面から開けるようにする(タブ、または「スコアカード」ボタン)。
- ホーム画面のラウンド一覧からも直接開けると良い。

### 12-2. ラウンド中のスコア確認

- ホール入力画面の ⋯ メニューに「**スコアカード**」を追加。
  タップすると 12-1 のスコアカードを、**プレー済みのホールまで**表示する
  (未プレーは「-」)。閉じると入力中のホールに戻る。入力中の内容は失われないこと。
- あわせて、ホール入力画面のヘッダーに**現在の累計**を常時表示する。
  例:`12H終了 +9`。省スペースで、ホール番号・Parの表示を邪魔しないこと。
- どちらもプレー中に頻繁に見るので、**2タップ以内**で到達できること。

---

## バッチ13:ゴルフ場と9ホールコースのデータモデル

### 背景

日本のゴルフ場は「9ホールのコースが3つ以上あり、その組み合わせで18ホールを回る」構成が多い。
例:恵庭カントリー倶楽部は27ホールで、摩周・阿寒・支笏の3コース(各9ホール・Par36)を
「摩周→阿寒」のように組み合わせて回る。

現在の実装は「コース = 18ホール固定」なので、この構成が表現できない。
組み合わせごとに別コースとして登録すると、同じ「摩周コース3番」の成績が
組み合わせごとにバラバラに集計されてしまい、コース別の比較ができない。

### 新しいデータモデル

```
Facility (ゴルフ場) { id, name }
  例: { name: "恵庭カントリー倶楽部" }

Loop (9ホールのコース) { id, facilityId, name, pars[9] }
  例: { facilityId: ..., name: "摩周コース", pars: [4,4,3,5,4,4,3,4,5] }

Round {
  id, date, facilityId, frontLoopId, backLoopId, tee,
  holes[18], playedHoles, complete
}

Hole { number(1-18), loopId, loopHole(1-9), par, shots[] }
```

- `holes[0..8]` は前半(frontLoop)のホール1〜9、`holes[9..17]` は後半(backLoop)のホール1〜9。
- `number` は通し番号(1〜18)で表示用。**集計は `loopId` + `loopHole` で行う**
  (同じコースを前半で回っても後半で回っても同じホールとして合算されるようにするため)。
- 18ホール固定のコース(例:札幌リージェントGC 新コース)は、
  **OUT/INの2つのLoopを持つFacility**として表現する。
  これにより既存の「OUT/INスタート」も自然に表現できる(INスタート = front:IN, back:OUT)。

### マイグレーション(非破壊・冪等であること)

既存の `Course { id, name, pars[18] }` を次のように変換する:

- Facility を作る:`{ id: "fac-" + course.id, name: course.name }`
- Loop を2つ作る:
  - `{ id: course.id + "-out", facilityId, name: "OUT", pars: course.pars.slice(0, 9) }`
  - `{ id: course.id + "-in",  facilityId, name: "IN",  pars: course.pars.slice(9, 18) }`

既存の `Round` は:

- `facilityId = "fac-" + round.courseId`
- `start === "IN"` なら `frontLoopId = <in>`, `backLoopId = <out>`、それ以外は逆。
- 各 `hole` に `loopId` / `loopHole` を補完する。既存の `hole.number` は実ホール番号
  (1〜18)なので、**`number <= 9` なら OUTループの `number` 番、`number >= 10` なら
  INループの `number - 9` 番**と決まる。
- **既存の `courseId` / `start` フィールドは削除せず残す**(問題が起きたときに戻せるように)。

プリセットの札幌リージェントGC 3コースも同じ形に変換する。

### 設定画面:既存の「コース管理」を作り替える(新規カードを足さない)

現在の設定画面には「コース管理」カードがあり、登録済みコースの一覧表示と
未使用コースの削除だけができる。**ここに別途「ゴルフ場登録」カードを足すと
役割が重複するので、既存の「コース管理」カードをそのまま作り替える。**

- カード名を「**ゴルフ場・コース管理**」にする。
- 一覧は**ゴルフ場ごと**に表示し、その下にそのゴルフ場の9ホールコースを並べる。
  各コースには使用ラウンド数を出す(現在の「◯ラウンドで使用中 / 未使用」の表示を踏襲)。
- このカードからできること:
  - **ゴルフ場の新規登録**(名前を入力)
  - **ゴルフ場にコース(9ホール)を追加**(コース名 + 9ホールぶんのPar)。
    1つのゴルフ場に**6つまで**登録できること。
  - **コース名・Parの編集**
  - **未使用のゴルフ場・コースの削除**(既存のルールを踏襲:
    ラウンドで使用中のものは削除不可、プリセットは削除不可)
- 簡易入力:コース追加時に「**18ホールのコースとして登録**」を選ぶと、
  OUT/INの2コースを一度に作れること(既存の18ホールコースと同じ形になる)。
- Parは既存のPar修正の仕組み(確認モーダル方式)を流用してよい。

### ラウンド開始画面

1. **ゴルフ場**を選ぶ(既存一覧 or 新規登録)
2. **前半のコース**と**後半のコース**を選ぶ(そのゴルフ場に登録されたコースから)。
   コースが2つしかないゴルフ場では、前半=OUT・後半=IN を既定で選択しておく。
   同じコースを前半・後半の両方に選ぶことも許可する(9ホールを2周する場合)。
3. **ティー**を選ぶ

既存の「OUT/INスタート」の選択UIは、前半・後半のコース選択に置き換えて廃止する。

### 既存ラウンドのコース付け替え(新モデル対応)

既に「恵庭カントリー倶楽部(18ホールのコース1つ)」として記録してしまったラウンドを、
あとから登録した「恵庭カントリー倶楽部 摩周→阿寒」に付け替えられるようにする。
これがないと、モデルを変えても過去の記録が正しいコースに紐づかない。

現在も振り返り画面のコース名タップでコースを付け替えられる(バッチ5-2で実装済み)ので、
**その機能を新モデルに合わせて拡張する**。新しく別の導線を作らない。

- 振り返り画面のコース名をタップすると、**ゴルフ場 → 前半コース → 後半コース**を
  選び直せるようにする。
- 確認ダイアログを挟んでから適用する(既存の挙動を踏襲)。
- 付け替え時、`holes[0..8]` を新しい前半コースの1〜9番、`holes[9..17]` を後半コースの
  1〜9番に割り当て直す(`loopId` / `loopHole` を更新)。
- **各ホールに記録済みのParは変更しない**(実際にプレーした記録を優先)。
  付け替え先のPar構成と食い違うホールがある場合は、確認ダイアログ内に
  「Par構成がコース情報と◯ホール分異なります(記録はそのまま保持されます)」と表示だけする
  (既存の挙動を踏襲)。
- 付け替え後、コース別・ホール別の集計に正しく反映されること。
- 複数のラウンドをまとめて付け替える機能までは不要。1ラウンドずつでよい。

### この段階では分析画面を変更しない

コース別・ホール別の集計は既存の `courseId` ベースのまま動かしておく
(バッチ14でループ単位に切り替える)。**このバッチの完了条件は、
新モデルでラウンドを開始・記録・保存でき、既存のラウンドもすべて開けること。**

---

## バッチ14:分析をコース(9ホール)単位に切り替える

- 累積ダッシュボードの「コース別・ホール別 平均の±」を、**ゴルフ場 → コース(9ホール)**の
  2段階にする。ゴルフ場を選ぶと、そのゴルフ場に登録された各コースの9ホールぶんの
  タイルが並ぶ。同じコースは**前半で回ったか後半で回ったかに関係なく合算**されること
  (これが今回の変更の核心。必ずテストデータで確認すること)。
- ホールタイルのラベルはコース内の番号(1〜9)にする。
- 絞り込み行の「コース」を「**ゴルフ場**」に変更し、さらに任意で**コース(9ホール)**でも
  絞り込めるようにする。ティー・ラウンド種別の絞り込みは現状のまま。
- スコアカード(バッチ12)のブロック見出しを、実際のコース名
  (例:「摩周コース」「阿寒コース」)に差し替える。
- 振り返り画面のヘッダーの表示も「ゴルフ場名・前半コース→後半コース」が分かる表記にする
  (例:`恵庭カントリー倶楽部 摩周→阿寒・白ティー`)。

---

## 共通条件(全バッチ共通)

- 作業開始前に作業ツリーがクリーンであることを確認する。
- 指示した範囲以外の画面・データ構造・集計は変更しない。
- **データのマイグレーションは必ず非破壊・冪等にする。**既存フィールドを削除せず、
  不足しているフィールドを補う形にする。2回走っても壊れないこと。
  既存データの削除・上書きが避けられない場合は、実装せずに WORKLOG.md に書いて止まる。
- 各バッチの動作確認には、**アップデート前の形式で保存されたデータを読み込んだ状態で
  全画面が壊れずに開けること**の確認を必ず含める。
- Service Worker のキャッシュバージョンは、ファイルを追加したバッチごとに上げること
  (バッチ11で v6 にしたあと、12以降でファイルが増えたら v7, v8… と上げる)。

---

## 受け入れ確認(全バッチ完了後に実施し、WORKLOG.md に記録)

1. **サーバを停止した完全オフライン状態**で、ラウンド開始→18ホール入力→保存→
   振り返り表示まで一通りできること。(最重要)
2. オフライン時に「オフライン」表示が出て、オンラインに戻ると消えること。
3. スコアカードが1画面(スクロールなし)に18ホール収まり、Par・対Par・パットが読めること。
   マス内が実打数ではなく対Par(ボギー=+1)になっていること。
4. ラウンド中に ⋯ からスコアカードを開いて閉じても、入力中のホールの内容が失われないこと。
5. 3コース以上のゴルフ場を新規登録し、「コースA→コースB」と「コースB→コースA」の
   2ラウンドを記録したとき、**コース別・ホール別の集計で同じコースの同じホールが
   合算されている**こと。
6. 旧モデルで記録したラウンドを、あとから登録した「ゴルフ場+前半コース+後半コース」に
   付け替えられ、付け替え後にコース別・ホール別の集計へ正しく反映されること。
   各ホールに記録済みのParが書き換わっていないこと。
7. 設定画面に「ゴルフ場・コース管理」が1つだけあり、
   ゴルフ場の登録・コースの追加・編集・削除がそこで完結すること
   (登録用と管理用でカードが二重になっていないこと)。
8. アップデート前に保存されていたラウンドがすべて開け、スコア・KPI・ヒートマップ・
   コース別分析の表示が壊れていないこと。
9. バックアップのエクスポート→インポートで、新モデルのデータが完全に復元できること。
